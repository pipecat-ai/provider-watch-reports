---
service: azure/tts
classes:
- AzureTTSService
- AzureHttpTTSService
date: 2026-09-01
pipecat_commit: 3f9756c1e
default_model: null
summary: Unchanged from yesterday — nothing is retiring, the HD/MAI voices already work through `Settings(voice=...)`, and the `voice_parameters` branch (capped last run, never published) is rebuilt and re-probed.
models_seen:
- DragonHDFlashLatestNeural (24 voices in eastus, GA)
- DragonHDLatestNeural (69 voices in eastus, GA + Preview)
- DragonHDOmniLatestNeural (15 voices in eastus, Preview)
- MAI-Voice-2 (47 voices in eastus, Preview)
- MAI-Voice-2-Flash (47 voices in eastus, Preview)
- standard neural (561 voices in eastus, includes the default en-US-SaraNeural, Status GA)
gaps:
- item: HD voice model parameters (temperature, top_p, top_k, cfg_scale, enhancePronunciation) are not reachable from Settings
  first_seen: 2026-08-31
  action: pr
- item: MAI voices emit no word boundaries, so AzureTTSService produces audio but no TTSTextFrame
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: '`AzureTTSService` sets `push_text_frames=False` and derives text frames from Azure''s word boundary events; an ad-hoc probe of Pipecat''s exact SSML got 13 / 13 / 10 events for `en-US-SaraNeural`, `en-US-Ava:DragonHDLatestNeural` and `en-US-Andrew:DragonHDOmniLatestNeural`, and 0 for `en-US-Ethan:MAI-Voice-2-Flash` (which still returned 223 KB of audio). Bot transcripts and word-timed UI go silent for the whole MAI family.'
  needs: a fallback (push the aggregated sentence when a synthesis completes with no word boundary) changes timing semantics for every voice — someone has to decide whether that fallback is unconditional or keyed to the voice family.
- item: Custom voice deployments are unreachable; the TTS classes have no endpoint_id
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: '`AzureSTTService` already takes `endpoint_id` and sets `SpeechConfig.endpoint_id`; the TTS classes only take `private_endpoint`, which is Private Link/custom domain, not a fine-tuned voice deployment.'
  needs: it is a constructor argument rather than a runtime setting, so it needs a maintainer's call on the shape (and on whether it pairs with `voice` validation).
- item: Personal voice / instant voice cloning is not reachable
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: the cloned voice is addressed with `<mstts:ttsembedding speakerProfileId='...'>` wrapping the text (supported by the MAI models, explicitly unsupported by DragonHD and DragonHDOmni); `_construct_ssml` has no element for it
  needs: it is gated access (Limited Access Review) and adds a nesting level to the SSML builder, so it needs a decision on whether Pipecat carries a feature most users cannot enable.
- item: The text streaming input API is not used, so synthesis starts only once a sentence is aggregated
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: '`SpeechSynthesisRequestInputType.TextStream` on the websocket v2 endpoint accepts LLM tokens as they arrive; Microsoft''s sample writes chunks into `request.input_stream`'
  needs: the streaming input accepts no SSML — only voice name and output format — so every `Settings` field this class exposes would be dropped on that path; whether that is a second mode or a replacement is a product call.
- item: The sentence-boundary silence is hard-coded at 20 ms
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: '`_construct_ssml` always emits `<mstts:silence type=''Sentenceboundary'' value=''20ms'' />`; Azure also defines Leading/Tailing/Comma-exact and the `-exact` variants, none of which a user can select, and the HD families ignore the element entirely'
  needs: the current value is a deliberate low-latency choice for agents, so exposing it means deciding what the default stays and which silence types are worth a field.
- item: An unlisted pipeline sample rate silently yields 24 kHz audio tagged with the pipeline's rate
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: '`sample_rate_to_output_format` falls back to `Raw24Khz16BitMonoPcm` for anything outside {8000, 16000, 22050, 24000, 44100, 48000}, while `TTSAudioRawFrame` is built with `self.sample_rate`, so the audio plays at the wrong speed instead of failing'
  needs: whether an unsupported rate should raise, resample, or keep today's silent fallback.
- item: The `azure` extra floor (`azure-cognitiveservices-speech>=1.47.0`) predates TTS fixes in 1.50 and 1.51
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: 1.50 fixed a TTS connection race condition and 1.51 fixed TTS token credential errors on the REST backend; 1.48.2 carries the CRL fix Microsoft required for Linux/Android by 2026-07-01. Fresh resolves get 1.51.2, so only pinned-old installs are exposed. Shared with azure/stt, which raised the same item.
  needs: whether provider-watch may move dependency floors, or whether that is a maintainer-owned change.
prs:
- branch: provider-watch/azure-tts-voice-parameters
  state: open
  summary: Add a voice_parameters setting to the Azure TTS services
  url: https://github.com/pipecat-ai/pipecat/pull/5522
  opened: '2026-09-01'
error: null
---

# Azure TTS — 2026-09-01

Nothing changed on either side in a day: the default `en-US-SaraNeural` is GA, no TTS retirement is scheduled, and the only actionable item is the `voice_parameters` branch that last run's PR cap held back — rebuilt and re-probed against the live API.

## What's new for Pipecat

The `eastus` catalogue is byte-for-byte yesterday's: 763 voices, 561 standard neural plus the four HD/MAI families. Exactly one voice carries `Status: Deprecated` — `en-GB-MiaNeural` — and it appears nowhere in `src/`, `examples/` or `tests/`, so nothing in Pipecat breaks with it. Azure has no model concept for TTS, so `default_model` stays null.

One docs/catalogue discrepancy worth knowing about: the HD voices page now advertises "700+ voices" for Dragon HD Omni and tells users to build a name by appending `:DragonHDOmniLatestNeural` to any voice, while the `eastus` voices list still returns only 15 Omni entries. Voice names pass straight through `Settings(voice=...)`, so this is Azure's rollout lagging its docs, not a gate in this service.

### PRs
- https://github.com/pipecat-ai/pipecat/pull/5522 — Add a voice_parameters setting to the Azure TTS services

### To consider
- **No text frames on MAI voices** (medium, since 2026-08-31) — `AzureTTSService` derives its `TTSTextFrame`s from Azure word boundary events (`push_text_frames=False`); `en-US-Ethan:MAI-Voice-2-Flash` returned 0 word boundaries where standard, DragonHD and DragonHDOmni voices returned 13, 13 and 10, so bot transcripts and word-timed UI disappear for the whole MAI family while audio still plays. — *Needs a call: a "no word boundary arrived, push the sentence" fallback changes timing semantics for every voice, so it has to be either unconditional or keyed to the voice family.*
- **Custom voice deployments** (medium, since 2026-08-31) — `AzureSTTService` accepts `endpoint_id` and sets it on the `SpeechConfig`; the TTS classes offer only `private_endpoint`, so a fine-tuned (professional) voice deployment cannot be addressed. — *Needs a call: it is a constructor argument, not a runtime setting, so the shape is a maintainer's decision.*
- **Personal voice** (low, since 2026-08-31) — instant voice cloning is addressed with `<mstts:ttsembedding speakerProfileId='...'>` around the text; the SSML builder has no element for it. — *Needs a call: the feature is behind Azure's Limited Access Review, so it is worth deciding whether Pipecat carries SSML most users cannot use.*
- **Token-level text streaming** (low, since 2026-08-31) — Azure's websocket v2 `TextStream` request type consumes LLM tokens directly instead of waiting for Pipecat's sentence aggregation. — *Needs a call: that path takes no SSML at all (voice name and output format only), so every setting this class exposes would be dropped on it.*
- **Sentence-boundary silence** (low, since 2026-08-31) — the 20 ms `<mstts:silence>` is hard-coded; Azure's other silence types and values are unreachable. — *Needs a call: 20 ms is a deliberate low-latency default, so exposing it means fixing what stays default and which types earn a field.*
- **Unlisted sample rates** (low, since 2026-08-31) — `sample_rate_to_output_format` quietly falls back to 24 kHz while frames carry the pipeline's rate, so a pipeline at, say, 32 kHz plays distorted audio rather than erroring. — *Needs a call: raise, resample, or keep the silent fallback.*
- **SDK floor** (low, since 2026-08-31) — `azure-cognitiveservices-speech>=1.47.0` allows versions without the 1.50 TTS connection-race fix, the 1.51 REST token-credential fix, and the 1.48.2 CRL fix Microsoft required by 2026-07-01. Fresh installs resolve to 1.51.2. — *Needs a call: whether provider-watch may raise dependency floors (same item as azure/stt).*

## Verification

| voice                                                                        | class                        | ok | latency      | note |
| ---------------------------------------------------------------------------- | ---------------------------- | -- | ------------ | ---- |
| en-US-SaraNeural (default)                                                     | AzureHttpTTSService          | ✅ | TTFB 147 ms  | clean shutdown, `TTSAudioRawFrame` + `TTSTextFrame` |
| en-US-SaraNeural (default)                                                     | AzureTTSService              | ✅ | TTFB 153 ms  | the harness pipeline never drains its `EndFrame` (nothing emits the `BotStoppedSpeakingFrame` that lifts this class's `pause_frame_processing`), so the run was killed and the latency comes from the service's own TTFB log line — same as last run |
| en-US-Ava:DragonHDLatestNeural, `voice_parameters=temperature=0.3;top_p=0.3`   | AzureHttpTTSService (branch) | ✅ | TTFB 2386 ms | branch build; accepted by the live API |
| en-US-Andrew:DragonHDOmniLatestNeural, all four Omni parameters                | AzureHttpTTSService (branch) | ✅ | TTFB 1026 ms | branch build; `temperature=0.7;top_p=0.8;top_k=22;cfg_scale=1.4` accepted |
| en-US-Ava:DragonHDLatestNeural, `voice_parameters=enhancePronunciation=true`   | AzureHttpTTSService (branch) | ✅ | TTFB 2236 ms | branch build; the boolean parameter form is accepted too |

The branch's SSML was also rendered offline: with `voice_parameters` unset the output is byte-identical to `main`'s, and with it set the `<voice>` element matches Microsoft's documented `parameters='top_p=0.8;top_k=22;temperature=0.7;cfg_scale=1.2'` form. `ruff format`, `ruff check` and `pytest tests/test_azure_tts.py tests/test_azure_stt.py` (19 tests) all pass on the branch.

Nothing from the 2026-08-31 report is unaccounted for: every gap it listed still holds, and its one branch entry (`capped: true`, so never published and absent from this checkout) is rebuilt here.

## Sources
- https://learn.microsoft.com/en-us/azure/ai-services/speech-service/high-definition-voices — page last updated 2026-07-07, unchanged since last run: DragonHD / DragonHDOmni / DragonHDFlash rosters; `<voice parameters>` carries `temperature` (0.3–1.0, default 0.7), `top_p` (0.3–1.0, 0.7), `top_k` (1–50, 22), `cfg_scale` (1.0–2.0, 1.4) and `enhancePronunciation` (default false); HD voices ignore `<prosody>`, `<emphasis>` and `<mstts:silence>`, and neither HD family supports `<mstts:ttsembedding>`; Omni supports word boundaries and `mstts:express-as`. Now advertises 700+ Omni voices via a name-suffix convention that the live catalogue does not yet reflect.
- https://learn.microsoft.com/en-us/azure/ai-services/speech-service/releasenotes — newest entries are still SDK 1.51 and 1.51.1 (July 2026); no August or September 2026 entry, no TTS retirement notice
- https://learn.microsoft.com/en-us/azure/ai-services/speech-service/mai-voices — MAI-Voice-2 and MAI-Voice-2-Flash (public preview), voice-name selection, styles via `mstts:express-as`, cloning via `<mstts:ttsembedding>` behind gated access; still no `parameters` attribute documented for this family
- https://docs.pipecat.ai/api-reference/server/services/tts/azure — matches the code: default voice `en-US-SaraNeural`, no model, the eight Azure settings fields
- `<region>.tts.speech.microsoft.com/cognitiveservices/voices/list` (ad-hoc, one call) — 763 voices in eastus, family counts above, `en-US-SaraNeural` Status GA, one `Deprecated` voice (`en-GB-MiaNeural`) that Pipecat never references
- probe.py sdk-versions — pin `azure-cognitiveservices-speech>=1.47.0,<2`, latest on PyPI 1.51.2 (2026-08-20)
- probe.py list-models — no OpenAI-compatible catalogue for Azure; the voices list endpoint is the catalogue
- `gh pr list --label provider-watch --search "azure tts"` — still no Azure TTS provider-watch PR, so the rebuilt branch is not a duplicate
