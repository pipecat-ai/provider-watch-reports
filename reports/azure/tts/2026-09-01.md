---
service: azure/tts
classes:
- AzureTTSService
- AzureHttpTTSService
date: 2026-09-01
pipecat_commit: 531ec8960
default_model: null
summary: Nothing is retiring, both classes synthesize cleanly against the live API, and the one actionable item is already PR #5522; the rest need maintainer calls.
models_seen:
- DragonHDFlashLatestNeural (24 voices in eastus, all GA)
- DragonHDLatestNeural (69 voices in eastus, 44 GA + 25 Preview)
- DragonHDOmniLatestNeural (15 voices in eastus, all Preview)
- MAI-Voice-2 (47 voices in eastus, all Preview)
- MAI-Voice-2-Flash (47 voices in eastus, all Preview)
- standard neural (561 voices in eastus, 552 GA + 8 Preview + 1 Deprecated; includes the default en-US-SaraNeural, Status GA)
gaps:
- item: HD voice model parameters (temperature, top_p, top_k, cfg_scale, enhancePronunciation) are not reachable from Settings
  first_seen: 2026-08-31
  action: pr
- item: MAI voices emit no word boundaries, so AzureTTSService produces audio but no TTSTextFrame
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: '`AzureTTSService` sets `push_text_frames=False` and derives text frames from Azure''s word boundary events. Re-probed today with Pipecat''s exact SSML: `en-US-Ethan:MAI-Voice-2-Flash` returned 240 KB of audio and 0 word boundaries, `en-US-SaraNeural` returned 14. Bot transcripts and word-timed UI go silent for the whole MAI family.'
  needs: a fallback (push the aggregated sentence when a synthesis completes with no word boundary) changes timing semantics for every voice — someone has to decide whether that fallback is unconditional or keyed to the voice family.
- item: Custom voice deployments are unreachable; the TTS classes have no endpoint_id
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: '`AzureSTTService` already takes `endpoint_id` and sets `SpeechConfig.endpoint_id`; the TTS classes only take `private_endpoint`, which is Private Link/custom domain, not a fine-tuned voice deployment.'
  needs: it is a constructor argument rather than a runtime setting, so it needs a maintainer's call on the shape (and on whether it pairs with `voice` validation).
- item: Personal voice / instant voice cloning is not reachable
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: the cloned voice is addressed with `<mstts:ttsembedding speakerProfileId='...'>` wrapping the text (supported by every MAI model, explicitly unsupported by DragonHD and DragonHDOmni); `_construct_ssml` has no element for it
  needs: it is gated access (Limited Access Review) and adds a nesting level to the SSML builder, so it needs a decision on whether Pipecat carries a feature most users cannot enable.
- item: The text streaming input API is not used, so synthesis starts only once a sentence is aggregated
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: '`SpeechSynthesisRequestInputType.TextStream` on the websocket v2 endpoint accepts LLM tokens as they arrive; Microsoft''s sample writes chunks into `request.input_stream`'
  needs: the streaming input accepts no SSML — only voice name and output format — so every `Settings` field this class exposes would be dropped on that path; whether that is a second mode or a replacement is a product call.
- item: The sentence-boundary silence is hard-coded at 20 ms
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: '`_construct_ssml` always emits `<mstts:silence type=''Sentenceboundary'' value=''20ms'' />`; Azure also defines Leading/Tailing/Comma-exact and the `-exact` variants, none of which a user can select, and both HD families ignore the element entirely'
  needs: the current value is a deliberate low-latency choice for agents, so exposing it means deciding what the default stays and which silence types are worth a field.
- item: An unlisted pipeline sample rate silently yields 24 kHz audio tagged with the pipeline's rate
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: '`sample_rate_to_output_format` falls back to `Raw24Khz16BitMonoPcm` for anything outside {8000, 16000, 22050, 24000, 44100, 48000}, while `TTSAudioRawFrame` is built with `self.sample_rate`, so the audio plays at the wrong speed instead of failing'
  needs: whether an unsupported rate should raise, resample, or keep today's silent fallback.
- item: The `azure` extra floor (`azure-cognitiveservices-speech>=1.47.0`) predates TTS fixes in 1.50 and 1.51
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: 1.50 fixed a TTS connection race condition and 1.51 fixed TTS token credential errors on the REST backend; 1.48.2 carries the CRL fix Microsoft required for Linux/Android by 2026-07-01. Fresh resolves get 1.51.2, so only pinned-old installs are exposed. Shared with azure/stt, which raised the same item.
  needs: whether provider-watch may move dependency floors, or whether that is a maintainer-owned change.
prs:
- url: https://github.com/pipecat-ai/pipecat/pull/5522
  state: open
  opened: '2026-09-01'
  summary: Add a voice_parameters setting to the Azure TTS services
error: null
---

# Azure TTS — 2026-09-01

Both classes are current: `en-US-SaraNeural` is GA, no TTS retirement is scheduled, and the only PR-shaped item is already open as #5522 — this run re-probed the live API and left no new branch.

## What's new for Pipecat

Nothing on the provider side moved. The `eastus` catalogue is still 763 voices (561 standard neural plus the four HD/MAI families), the SDK release notes still end at 1.51.1 (July 2026), and the HD and MAI docs pages carry the same content as last run. Azure has no model concept for TTS, so `default_model` stays null.

Two things checked this run that turned out not to be gaps:

- The SSML reference now documents HD styles and paralinguistics (`[laughter]`, `[whispering]`, 60-plus style markers) for DragonHD, Omni and Flash. Styles are reachable through `Settings(style=...)`, which `_construct_ssml` renders as `mstts:express-as`, and the bracket markers survive `_escape_text` untouched, so both input forms already work.
- MAI's 15 languages / 18 locales — including `hu-HU`, `ro-RO`, `th-TH`, `tr-TR` and `nl-NL` — are all present in `language_to_azure_language`, so no locale is blocked by the mapping.

The catalogue's one `Deprecated` voice, `en-GB-MiaNeural`, still appears nowhere in `src/`, `examples/` or `tests/`.

### PRs
- https://github.com/pipecat-ai/pipecat/pull/5522 — Add a voice_parameters setting to the Azure TTS services (open)

### To consider
- **No text frames on MAI voices** (medium, since 2026-08-31) — `AzureTTSService` derives its `TTSTextFrame`s from Azure word boundary events (`push_text_frames=False`); re-probed today, `en-US-Ethan:MAI-Voice-2-Flash` returned 0 word boundaries against 14 for the default voice, so bot transcripts and word-timed UI disappear for the whole MAI family while audio still plays. — *Needs a call: a "no word boundary arrived, push the sentence" fallback changes timing semantics for every voice, so it has to be either unconditional or keyed to the voice family.*
- **Custom voice deployments** (medium, since 2026-08-31) — `AzureSTTService` accepts `endpoint_id` and sets it on the `SpeechConfig`; the TTS classes offer only `private_endpoint`, so a fine-tuned (professional) voice deployment cannot be addressed. — *Needs a call: it is a constructor argument, not a runtime setting, so the shape is a maintainer's decision.*
- **Personal voice** (low, since 2026-08-31) — instant voice cloning is addressed with `<mstts:ttsembedding speakerProfileId='...'>` around the text; the SSML builder has no element for it. — *Needs a call: the feature is behind Azure's Limited Access Review, so it is worth deciding whether Pipecat carries SSML most users cannot use.*
- **Token-level text streaming** (low, since 2026-08-31) — Azure's websocket v2 `TextStream` request type consumes LLM tokens directly instead of waiting for Pipecat's sentence aggregation. — *Needs a call: that path takes no SSML at all (voice name and output format only), so every setting this class exposes would be dropped on it.*
- **Sentence-boundary silence** (low, since 2026-08-31) — the 20 ms `<mstts:silence>` is hard-coded; Azure's other silence types and values are unreachable. — *Needs a call: 20 ms is a deliberate low-latency default, so exposing it means fixing what stays default and which types earn a field.*
- **Unlisted sample rates** (low, since 2026-08-31) — `sample_rate_to_output_format` quietly falls back to 24 kHz while frames carry the pipeline's rate, so a pipeline at, say, 32 kHz plays distorted audio rather than erroring. — *Needs a call: raise, resample, or keep the silent fallback.*
- **SDK floor** (low, since 2026-08-31) — `azure-cognitiveservices-speech>=1.47.0` allows versions without the 1.50 TTS connection-race fix, the 1.51 REST token-credential fix, and the 1.48.2 CRL fix Microsoft required by 2026-07-01. Fresh installs resolve to 1.51.2. — *Needs a call: whether provider-watch may raise dependency floors (same item as azure/stt).*

## Verification

| voice | class | ok | latency | note |
| --- | --- | --- | --- | --- |
| en-US-SaraNeural (default) | AzureHttpTTSService | ✅ | TTFB 583 ms (median of 3, 528–888 ms) | clean shutdown, `TTSAudioRawFrame` + `TTSTextFrame` |
| en-US-SaraNeural (default) | AzureTTSService | ✅ | TTFB 361 ms | audio and word timings fine (TTFA 497 ms); the harness pipeline never drains its `EndFrame` because nothing emits the `BotStoppedSpeakingFrame` that lifts this class's `pause_frame_processing`, so the run was killed and the latency comes from the service's own TTFB log line — same harness limitation as the last two runs |
| en-US-Ethan:MAI-Voice-2-Flash | ad-hoc SSML probe | ✅ | — | 240 KB audio, **0** word boundaries with Pipecat's exact SSML |
| en-US-SaraNeural | ad-hoc SSML probe | ✅ | — | 222 KB audio, 14 word boundaries — the control for the row above |

Nothing from the 2026-08-31 report is unaccounted for: every gap it listed still holds against `531ec8960`, and its branch entry is now PR #5522.

## Sources
- https://learn.microsoft.com/en-us/azure/ai-services/speech-service/releasenotes — newest entries are still SDK 1.51.1 and 1.51 (July 2026); the 2026 retirements are ConversationTranslator and MeetingTranscriber samples, neither of which Pipecat uses; no TTS retirement notice
- https://learn.microsoft.com/en-us/azure/ai-services/speech-service/high-definition-voices — page updated 2026-07-07, unchanged: `<voice parameters>` carries `temperature` (0.3–1.0, default 0.7), `top_p` (0.3–1.0, 0.7), `top_k` (1–50, 22), `cfg_scale` (1.0–2.0, 1.4) and `enhancePronunciation` (default false); HD voices ignore `<prosody>`, `<emphasis>` and `<mstts:silence>`, and neither HD family supports `<mstts:ttsembedding>`; Omni supports word boundary events
- https://learn.microsoft.com/en-us/azure/ai-services/speech-service/speech-synthesis-markup-voice — page updated 2026-08-18; HD styles and paralinguistics are expressible three ways (`mstts:express-as`, inline `[style]` markers, plain text), all of which Pipecat's SSML builder already permits
- https://learn.microsoft.com/en-us/azure/ai-services/speech-service/mai-voices — page updated 2026-07-23; MAI-Voice-2 and MAI-Voice-2-Flash still public preview, voice-name selection, styles via `mstts:express-as`, cloning via `<mstts:ttsembedding>` behind gated access; still no `parameters` attribute documented for this family
- https://docs.pipecat.ai/api-reference/server/services/tts/azure — matches the code: default voice `en-US-SaraNeural`, no model, the eight Azure settings fields
- `<region>.tts.speech.microsoft.com/cognitiveservices/voices/list` (ad-hoc, one call) — 763 voices in eastus, family and status counts above, `en-US-SaraNeural` Status GA, one `Deprecated` voice (`en-GB-MiaNeural`)
- probe.py sdk-versions — pin `azure-cognitiveservices-speech>=1.47.0,<2`, latest on PyPI 1.51.2 (2026-08-20)
- probe.py list-models — no OpenAI-compatible catalogue for Azure; the voices list endpoint is the catalogue
- `gh pr list --label provider-watch --search "azure tts"` — one PR, #5522, open; nothing else to dedupe against
