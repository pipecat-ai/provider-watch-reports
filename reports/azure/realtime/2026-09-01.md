---
service: azure/realtime
classes: [AzureRealtimeLLMService]
date: 2026-09-01
pipecat_commit: 3f9756c1e
default_model: gpt-realtime-2.1 (inherited from OpenAIRealtimeLLMService; on Azure the value must name the deployment)
summary: Unchanged from 2026-08-31 — the class matches Azure's GA v1 realtime surface, while the example bot and CLI scaffold still never name a deployment and the scaffold's transcription config is still silently dropped; no live probe possible (the run environment's Azure resource does not resolve).
models_seen:
  - gpt-4o-mini-realtime-preview (2024-12-17)
  - gpt-4o-realtime-preview (2024-12-17)
  - gpt-live-transcribe (2026-07-29)
  - gpt-realtime (2025-08-28)
  - gpt-realtime-1.5 (2026-02-23)
  - gpt-realtime-2 (2026-05-06, preview, retired 2026-08-31)
  - gpt-realtime-2.1 (2026-07-07, preview)
  - gpt-realtime-2.1-mini (2026-07-07, preview)
  - gpt-realtime-mini (2025-10-06)
  - gpt-realtime-mini (2025-12-15)
  - gpt-realtime-translate (2026-05-06)
  - gpt-realtime-whisper (2026-05-06)
gaps:
  - item: Example bot and CLI scaffold never set Settings.model, so the URL carries OpenAI's default gpt-realtime-2.1 instead of the Azure deployment
    first_seen: 2026-08-31
    action: consider
    priority: high
    note: "`examples/realtime/realtime-azure.py` and the `azure_realtime` entries in `src/pipecat/cli/registry/_configs.py` / `service_metadata.py` build the service with no `model`, so the base class appends `?model=gpt-realtime-2.1` (`openai/realtime/llm.py:334`). On Azure that query parameter is the deployment name, not a model name, so a fresh bot only connects if the user happened to name their deployment `gpt-realtime-2.1`."
    needs: which env var should name the deployment (the scaffold's env template emits only `AZURE_REALTIME_API_KEY` and `AZURE_REALTIME_BASE_URL`), and whether the service should refuse to connect when no deployment is named
  - item: CLI scaffold passes `input_audio_transcription=` to SessionProperties, a field the event model no longer has
    first_seen: 2026-08-31
    action: consider
    priority: medium
    note: "`SessionProperties(input_audio_transcription=InputAudioTranscription(model=\"whisper-1\"))` dumps to `{'type': 'realtime'}` — the field is not in `SessionProperties.model_fields` and pydantic drops the kwarg — so scaffolded Azure bots run with no user transcription. The shape the model accepts is `audio=AudioConfiguration(input=AudioInput(transcription=...))`, as `examples/realtime/realtime-azure.py` uses. On Azure the transcription `model` must also name a transcription deployment, not the literal `whisper-1`."
    needs: the scaffold needs a transcription deployment name from somewhere, so fixing the shape alone still leaves a literal that only works by coincidence
  - item: Reasoning settings are stripped for Azure deployments of gpt-realtime-2/2.1 because the check reads the deployment name
    first_seen: 2026-08-31
    action: consider
    priority: medium
    note: "`OpenAIRealtimeLLMService._strip_unsupported_reasoning` keeps `reasoning` only when `settings.model` contains one of `_REASONING_CAPABLE_MODEL_SUBSTRINGS` (`gpt-realtime-2`, `gpt-realtime-2.1`). On Azure that string is the deployment name, so a reasoning-capable deployment called anything else silently loses `Reasoning(effort=...)` (with a warning)."
    needs: how Azure should decide reasoning capability when the model name is opaque — trust the user (no stripping on Azure) or add an explicit opt-in
  - item: Azure's realtime transcription and translation session types are not reachable
    first_seen: 2026-08-31
    action: consider
    priority: low
    note: Azure exposes dedicated flows for `gpt-realtime-translate` (`/openai/v1/realtime/translations`) and for transcription (`session.type = "transcription"`, models `gpt-realtime-whisper`, `gpt-live-transcribe`). The service only speaks the voice-agent session.
    needs: a call on whether these belong in Pipecat as a realtime STT/translation service rather than in this LLM class
  - item: docs.pipecat.ai has no page for AzureRealtimeLLMService
    first_seen: 2026-08-31
    action: consider
    priority: low
    note: The unit's `docs_url` (`/api-reference/server/services/llm/azure`) documents only `AzureLLMService`, and `/api-reference/server/services/s2s/azure` is a 404, so the deployment-name rule for `Settings.model` and the v1-endpoint requirement live only in the class docstring.
    needs: the docs site is a separate repo, so this needs an owner there rather than a change here
prs: []
error: AZURE_REALTIME_BASE_URL points at a resource host that does not resolve (DNS failure under .cognitiveservices.azure.com), and it is still in the retired `/openai/realtime?api-version=...&deployment=...` form, so no connect probe could run.
---

# Azure Realtime — 2026-09-01

No change on either side since 2026-08-31: Azure published nothing new for realtime, and all five known gaps still reproduce in the code. Nothing to review; the open items are the same maintainer calls as yesterday.

## What's new for Pipecat

### PRs
Nothing.

### To consider
- **Deployment name never set** (high, since 2026-08-31) — `examples/realtime/realtime-azure.py` and the `azure_realtime` CLI scaffold construct `AzureRealtimeLLMService` with no `Settings(model=...)`, so the base class appends OpenAI's default `?model=gpt-realtime-2.1`. On Azure that query parameter names the user's *deployment*, so a fresh bot connects only if the deployment happens to carry that name. — *Needs a call: which env var names the deployment (the scaffold's env template emits only `AZURE_REALTIME_API_KEY` and `AZURE_REALTIME_BASE_URL`), and whether the service should refuse to connect when no deployment is named.*
- **Scaffold's transcription config is dropped** (medium, since 2026-08-31) — the `azure_realtime` scaffold emits `SessionProperties(input_audio_transcription=InputAudioTranscription(model="whisper-1"))`; that field is not on `SessionProperties`, pydantic ignores it (`model_dump(exclude_none=True)` → `{'type': 'realtime'}`), and generated bots get no user transcription. The accepted shape is `audio=AudioConfiguration(input=AudioInput(transcription=...))`. — *Needs a call: on Azure the transcription model is itself a deployment name, so fixing the shape still leaves `whisper-1` working only by coincidence.*
- **Reasoning stripped on Azure** (medium, since 2026-08-31) — `_strip_unsupported_reasoning` recognizes reasoning-capable models by substring on `Settings.model`, which on Azure is an opaque deployment name; a `gpt-realtime-2.1` deployment named anything else loses its `Reasoning(effort=...)` before `session.update`. — *Needs a call: whether Azure should skip the capability check entirely or gate it on something explicit.*
- **Transcription/translation sessions** (low, since 2026-08-31) — Azure ships `gpt-realtime-translate`, `gpt-realtime-whisper` and `gpt-live-transcribe` behind a translation flow (`/openai/v1/realtime/translations`) and `session.type = "transcription"`; neither is reachable from this class. — *Needs a call: these are arguably a separate realtime STT/translation service, not this LLM class.*
- **No API-reference page** (low, since 2026-08-31) — the unit's `docs_url` covers only `AzureLLMService`, and `s2s/azure` 404s, so nothing on docs.pipecat.ai tells users that `Settings.model` is the deployment name or that dated `api-version` URLs don't work. — *Needs a call: the docs site is a separate repo, so this needs an owner there.*

Not gaps, checked this run: `gpt-realtime-2` (2026-05-06 preview) passed its retirement date on 2026-08-31, but Pipecat hard-codes it nowhere for Azure — it appears only in the base class's `_REASONING_CAPABLE_MODEL_SUBSTRINGS`, where it is a substring test, not a request value; `gpt-realtime-2.1`/`gpt-realtime-2.1-mini` (preview) run to 2027-06-25 and `gpt-realtime`/`gpt-realtime-1.5` are GA into 2027, so the inherited default still names a real Azure model — it remains the wrong thing to send because the parameter is a deployment name; the Entra ID scope in the docstring (`https://ai.azure.com/.default`), the `api-key` header, the `/openai/v1/realtime` path and the preview-URL warning all still match Azure's current guidance ("use the GA endpoint with `/openai/v1` in the URL. Don't use date-based API versions"); the `azure` extra pins only `azure-cognitiveservices-speech` (latest 1.51.2, inside `>=1.47.0,<2`), which the realtime path does not use. No digest-issue comment concerned this unit, so no decisions file exists.

## Verification

| model | class | ok | latency | note |
| ----- | ----- | -- | ------- | ---- |
| — | AzureRealtimeLLMService | ❌ | — | `probe.py run` fails before any network call: the unit has no CLI-registry entry, so probe.py passes neither `base_url` nor `api_key` — `constructor: TypeError("AzureRealtimeLLMService.__init__() missing 1 required keyword-only argument: 'base_url'")` |
| deployment from `AZURE_REALTIME_BASE_URL` | AzureRealtimeLLMService | ❌ | — | connect check not attempted: the resource host in the env var does not resolve (`getaddrinfo` → `gaierror`, second run in a row), and the URL is still the retired `/openai/realtime?api-version=…&deployment=…` preview form |
| — | `SessionProperties` (offline) | ✅ | — | `SessionProperties(input_audio_transcription=InputAudioTranscription(model="whisper-1")).model_dump(exclude_none=True)` → `{'type': 'realtime'}`; `'input_audio_transcription' in SessionProperties.model_fields` is `False` |

## Sources
- https://learn.microsoft.com/en-us/azure/foundry/openai/how-to/realtime-audio — supported realtime models (page still dated 2026-07-31, unchanged since last run; newest listed `gpt-realtime-2`), "use the GA endpoint with `/openai/v1` in the URL. Don't use date-based API versions", session types `realtime`/`transcription`, translation flow on `/openai/v1/realtime/translations`, deployment named via `AZURE_OPENAI_DEPLOYMENT_NAME`, 60-minute session cap
- https://learn.microsoft.com/en-us/azure/foundry/openai/concepts/model-retirement-schedule — still updated 2026-08-27; `gpt-realtime-2` (preview) retirement date 2026-08-31 now passed, `gpt-realtime-2.1`/`-2.1-mini` (preview) to 2027-06-25, `gpt-realtime` to 2027-03-02, `gpt-realtime-1.5` to 2027-08-24, earliest `gpt-realtime-mini` row 2026-09-21
- https://docs.pipecat.ai/api-reference/server/services/llm/azure — documents `AzureLLMService` only; `/api-reference/server/services/s2s/azure` returns 404
- probe.py list-models --provider azure — "no OpenAI-compatible base_url known"; Azure has no global catalogue (deployments are per-resource), so models come from the docs
- probe.py sdk-versions --provider azure — `azure-cognitiveservices-speech` pinned `>=1.47.0,<2`, latest 1.51.2 (2026-08-20), inside the pin and unused by the realtime path
- src/pipecat/services/openai/realtime/llm.py — inherited default `gpt-realtime-2.1` (line 292), URL built as `f"{base_url}?model={model}"` (line 334), `_REASONING_CAPABLE_MODEL_SUBSTRINGS` substring check (line 775)
- src/pipecat/cli/templates/server/env.example.jinja2 — scaffold emits only `AZURE_REALTIME_API_KEY` and `AZURE_REALTIME_BASE_URL`, no deployment variable
- gh pr list --label provider-watch --search "azure realtime" --state all — empty; no prior provider-watch PRs for this unit
