---
service: xai/realtime
classes:
- GrokRealtimeLLMService
date: 2026-09-01
pipecat_commit: 531ec8960
default_model: grok-voice-latest
summary: The default is current; a one-character sample-rate typo blocks xAI's 22050 Hz PCM rate and is fixed on a branch.
models_seen:
- grok-voice-latest
- grok-voice-think-fast-1.0
- grok-voice-think-fast-2.0
gaps:
- item: '`SUPPORTED_SAMPLE_RATES` lists 21050 instead of xAI''s 22050'
  first_seen: 2026-09-01
  action: pr
- item: '`audio.input.transport` / `audio.output.transport` accept "binary" but the service only speaks the JSON wire path'
  first_seen: 2026-09-01
  action: consider
  priority: medium
  note: assistant audio is emitted only on the configured `output.transport`, so setting it to "binary" leaves `parse_server_event` unable to parse the frames and the conversation silent; `_send_user_audio` likewise always base64-encodes into `input_audio_buffer.append`
  needs: a call on whether to implement binary framing or drop `transport` from the settings so the field cannot be set to a value the service does not honor
- item: SIP calls are unreachable because the WebSocket URL is always built with `model` and never `call_id`
  first_seen: 2026-09-01
  action: consider
  priority: medium
  note: the service already handles `input_audio_buffer.dtmf_event_received`, which xAI emits on SIP sessions only, so the DTMF path cannot currently be exercised; joining a call needs `wss://api.x.ai/v1/realtime?call_id=<id>` with no `model`
  needs: a constructor or settings shape for `call_id` (it is per-call, not per-service) — more than a pass-through field
- item: '`audio/opus` is not among the accepted codecs'
  first_seen: 2026-09-01
  action: consider
  priority: low
  note: xAI accepts `audio/opus` (24 kHz mono, one raw packet per payload) for input and output; Pipecat models only PCM, PCMU and PCMA
  needs: Opus packets would have to be decoded before they can become `TTSAudioRawFrame`, so this is more than a new format class
prs:
- branch: provider-watch/xai-realtime-pcm-22050
  state: open
  summary: Accept Grok Voice Agent's 22050 Hz PCM sample rate
  url: https://github.com/pipecat-ai/pipecat/pull/5560
  opened: '2026-09-01'
error: null
---

# xAI Realtime — 2026-09-01

`grok-voice-latest` is still xAI's recommended alias and needs no change; the one clear defect is a sample-rate typo, fixed on a branch.

## What's new for Pipecat

### PRs
- https://github.com/pipecat-ai/pipecat/pull/5560 — Accept Grok Voice Agent's 22050 Hz PCM sample rate

### To consider
- **Binary audio transport** (medium, since 2026-09-01) — `AudioInput.transport` / `AudioOutput.transport` expose xAI's `"binary"` wire path, but the receive loop parses every message as JSON and `_send_user_audio` always base64-encodes into `input_audio_buffer.append`. Since assistant audio is emitted only on the configured `output.transport`, a user who sets `"binary"` gets warnings and no audio. — *Needs a call: implement binary framing, or drop `transport` from the settings so the field cannot be set to a value the service does not honor.*
- **SIP `call_id`** (medium, since 2026-09-01) — `_connect` always builds `?model=<model>`; joining an inbound SIP call requires `?call_id=<id>` (where `model` is ignored). The service already handles `input_audio_buffer.dtmf_event_received`, which xAI emits on SIP sessions only, so that path is unreachable today. — *Needs a call: `call_id` is per-call rather than per-service, so it needs a constructor or settings shape rather than a pass-through field.*
- **`audio/opus` codec** (low, since 2026-09-01) — xAI accepts `audio/opus` (24 kHz mono, one raw Opus packet per JSON delta or binary frame) for both directions; `events.py` models PCM, PCMU and PCMA only. — *Needs a call: Opus packets must be decoded before becoming `TTSAudioRawFrame`, so a new format class alone would not make it work.*

Everything else in xAI's documented `session.update` schema — `instructions`, `voice`, `reasoning.effort`, all four `turn_detection` fields, `resumption.enabled`, `replace`, `audio.output.speed`, `audio.input.transcription.language_hint` / `keyterms`, and all five tool types — is already reachable from `SessionProperties`, as is every server event xAI documents.

## Verification

| model                     | class                  | ok | latency                          | note |
| ------------------------- | ---------------------- | -- | -------------------------------- | ---- |
| grok-voice-latest         | GrokRealtimeLLMService | ✅ | n/a (connect-only probe)         | session.update accepted |
| grok-voice-think-fast-2.0 | GrokRealtimeLLMService | ✅ | n/a (connect-only probe)         | what the alias resolves to |
| grok-voice-think-fast-1.0 | GrokRealtimeLLMService | ✅ | n/a (connect-only probe)         | previous generation, still connects |
| grok-voice-latest (branch)| GrokRealtimeLLMService | ✅ | n/a (connect-only probe)         | branch sources on `PYTHONPATH` |

Ad-hoc `session.update` against `wss://api.x.ai/v1/realtime` (two connections, no audio sent): PCM `rate: 21050` — the value `SUPPORTED_SAMPLE_RATES` allows today — is refused with `invalid_event`, which drops the whole session config; `rate: 22050` returns `session.updated` echoing the rate back. A third connection built the payload from the branch's `SessionProperties`/`PCMAudioFormat` at 22050 Hz and was likewise accepted.

## Sources
- https://docs.x.ai/llms.txt — full docs as text; the source for the Speech-to-Speech guide, SIP guide, model list and REST/WS reference below
- https://docs.x.ai/developers/model-capabilities/audio/speech-to-speech — session parameter table (PCM rates 8000/16000/22050/24000/32000/44100/48000, `audio/opus` codec, `transport` json/binary semantics, `replace`, `resumption`, `speed`), model table (`grok-voice-latest` = `grok-voice-think-fast-2.0`; `grok-voice-think-fast-1.0` previous generation)
- https://docs.x.ai/developers/model-capabilities/audio/speech-to-speech/sip — SIP sessions join with `?call_id=`, where `model` is ignored
- https://docs.x.ai/developers/rest-api-reference/inference/voice — full client/server message list; every event is handled in `events.py`'s `_server_event_types`
- https://docs.x.ai/developers/release-notes — only one 2026 voice entry: `grok-voice-think-fast-2.0`, with `grok-voice-latest` routing to it from 2026-08-05; no voice retirements
- https://docs.x.ai/developers/model-capabilities/audio/text-to-speech — voice roster shared with Speech to Speech; `eve` is still the default, matching `SessionProperties.voice`
- probe.py list-models — the xAI catalogue endpoint returns chat/image models only, no voice models; the model list above comes from the docs
- probe.py sdk-versions — the `xai` extra pulls in no SDK of its own; `GrokRealtimeLLMService` talks to the API over `websockets` directly, so the repo-wide `openai>=1.74.0,<3` pin (3.6.0 on PyPI) does not affect this unit
- https://github.com/pipecat-ai/pipecat/pull/5199 — closed unmerged, but the session fields it described (turn_detection tuning, transcription hints, output speed, reasoning effort) are all present in the code today, so nothing is outstanding from it
