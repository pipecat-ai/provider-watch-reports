---
service: mistral/tts
classes: [MistralTTSService]
date: 2026-09-14
pipecat_commit: f6d601303
default_model: voxtral-mini-tts-2603
summary: Current — no new TTS model, nothing retiring, mistralai 2.10.0 leaves the speech API untouched; the same two affordance gaps carry forward.
models_seen:
  - voxtral-mini-tts-2603
  - voxtral-mini-tts-latest
gaps:
  - item: Mistral's one-off reference audio (`ref_audio`, zero-shot voice cloning) is not reachable from Settings
    first_seen: 2026-09-01
    action: consider
    priority: medium
    note: "the speech request takes `ref_audio` (base64 clip) as an alternative to `voice_id`; a Settings field passed into `complete_async` would cover it"
    needs: "`run_tts` calls `assert_given(self._settings.voice)`, so voice is mandatory today; exposing `ref_audio` needs a stance on making voice optional and on which wins when both are set"
  - item: "`language` is accepted in MistralTTSSettings but never sent"
    first_seen: 2026-09-01
    action: consider
    priority: low
    note: Mistral's speech endpoint has no language parameter (the voice and the input text decide it); the service stores `language=None` per the TTSSettings convention while the MistralTTSSettings docstring still advertises the field
    needs: whether unsupported inherited fields should be documented as no-ops here or dropped from the docstring
prs: []
error: null
---

# Mistral TTS — 2026-09-14

Current: `voxtral-mini-tts-2603` is still the only TTS model Mistral serves, it works, and nothing is retiring. No branch.

## What's new for Pipecat

### PRs
Nothing.

### To consider
- **Reference-audio voice cloning** (medium, since 2026-09-01) — the speech request accepts `ref_audio`, a base64 clip that clones a voice zero-shot without first creating a saved voice; `MistralTTSSettings` has no field for it, so Pipecat users must pre-register a voice through `audio.voices.create`. A field plus a pass-through in `run_tts` is the shape. — *Needs a call: `run_tts` requires a voice (`assert_given(self._settings.voice)`), so exposing `ref_audio` means making voice optional and deciding precedence when both are set.*
- **`language` is a no-op** (low, since 2026-09-01) — Mistral's speech endpoint has no language parameter, so the field inherited from `TTSSettings` is stored and never sent, while the `MistralTTSSettings` docstring still lists it as "Language for speech synthesis". — *Needs a call: whether to document the field as unsupported here or drop it from the docstring.*

## Verification

| model                 | class             | ok | latency     | note                                                            |
| --------------------- | ----------------- | -- | ----------- | --------------------------------------------------------------- |
| voxtral-mini-tts-2603 | MistralTTSService | ✅ | TTFB 845 ms | voice set to the UUID hard-coded in `examples/voice/voice-mistral.py`; still resolves |

Single sample, consistent with the 708/937 ms pair from 2026-09-01 and with Mistral's "~0.8 s end-to-end for PCM" figure. No comparison depended on it.

## Sources
- probe.py list-models --provider mistral — catalogue still has exactly two TTS entries, `voxtral-mini-tts-2603` and `voxtral-mini-tts-latest`; our default is the dated pin of the alias
- probe.py sdk-versions --provider mistral — `mistralai` 2.10.0 on PyPI (2026-09-09), inside the `>=2.0.0,<3` pin; 2.9.3 installed
- https://github.com/mistralai/client-python/releases — 2.10.0 is a large regeneration (campaign/chat-event endpoints dropped, service accounts added, `Connector` became a union) but its only audio change is nullable transcription segment timestamps; nothing touches speech
- `mistralai` 2.10.0 wheel, `client/models/speechrequest.py` and `client/models/speechstream*.py` — `SpeechRequest` fields and the `SpeechStreamAudioDelta` / `SpeechStreamDone` types our service imports are byte-for-byte the same as 2.9.3, so the in-pin SDK bump is not a break
- https://docs.mistral.ai/resources/changelogs — latest entry is OCR 4.1 GA (2026-08-31); no audio or TTS entries since the March Voxtral TTS release
- https://docs.mistral.ai/getting-started/models/models_overview/ — Voxtral TTS 26.03 listed as active; the deprecation table names only `voxtral-mini-2507` and `voxtral-mini-transcribe-2507` (both retired 2026-05-31), nothing for TTS
- https://docs.mistral.ai/capabilities/audio/text_to_speech/ — prosody and emotion are carried by the voice prompt, not by request parameters; confirms there is no style/speed knob to expose
- https://docs.mistral.ai/studio/audio/text_to_speech/speech — documented request fields remain `input`, `model`, `voice_id`, `ref_audio`, `response_format` (plus `stream`, `metadata`)
- WebSearch "Mistral Voxtral TTS announcement September 2026" — nothing newer than the March 2026 launch; no successor model announced
- https://docs.pipecat.ai/api-reference/server/services/tts/mistral — documents the same default and settings as the code
