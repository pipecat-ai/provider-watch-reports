---
service: bland/tts
classes: [BlandTTSService, BlandHttpTTSService]
date: 2026-09-01
pipecat_commit: 3f9756c1e
default_model: null
summary: Bland's v2 TTS surface is unchanged and both classes still cover it in full; no live probe ran because BLAND_API_KEY is not set.
models_seen:
  - btts-2
  - btts-3
gaps: []
prs: []
error: BLAND_API_KEY is not set, so no live probe could run
---

# Bland TTS — 2026-09-01

Nothing to change: Bland's v2 TTS schema, model list and endpoints are the same as last run,
and both classes already send every documented field. Still unverified live.

## What's new for Pipecat

### PRs

Nothing.

### To consider

Nothing.

## Notes

- **Nothing new from the provider.** Bland's changelog still ends at BTTS Experimental
  (2026-04-30) and the Bland Speech launch (2026-03-11). `GET /v2/tts/models` still lists
  `btts-3` (recommended) and `btts-2` (legacy), and the model remains a property of the voice
  rather than a request field, so `default_model: null` stays correct.
- **Request schema still fully covered.** HTTP `/v2/tts` takes `text`, `voice`,
  `audio.{encoding,sample_rate,container}` and `controls.{expressiveness,stability}`; the
  WebSocket `init` takes `voice`, `audio` and the same two controls, with `speak`,
  `end_of_turn`, `cancel` and `close` as the client messages. `BlandTTSSettings` plus the
  request builders cover all of it. The only documented value not sent is
  `audio.encoding: mulaw`, which Pipecat cannot use — `TTSAudioRawFrame` carries linear PCM
  and telephony serializers do the μ-law conversion at the wire.
- **Turn limits are handled.** The WebSocket caps a turn at 4,000 characters and reports
  `context_overflow`; `_read_until_closed` abandons the turn and closes its audio context on
  any error code, so a long turn ends cleanly rather than hanging. HTTP's 5,000-character
  limit surfaces through the v2 error envelope.
- **No SDK.** The `bland` extra has no dependencies; both classes speak HTTP/WebSocket
  directly, so there is no pin to fall behind.

## Verification

| model | class               | ok | latency | note                                 |
| ----- | ------------------- | -- | ------- | ------------------------------------ |
| —     | BlandTTSService     | ⚠️ | —       | not probed: BLAND_API_KEY is not set |
| —     | BlandHttpTTSService | ⚠️ | —       | not probed: BLAND_API_KEY is not set |

`probe.py list-models --provider bland` reports no catalogue for this provider; the model list
above comes from Bland's List TTS Models reference.

## Sources

- https://www.bland.ai/changelog — latest TTS entries remain BTTS Experimental (2026-04-30) and Bland Speech (2026-03-11); nothing new since the last run
- https://docs.bland.ai/api-v2/post/tts-ws — WebSocket schema unchanged: `init`/`speak`/`end_of_turn`/`cancel`/`close`, `controls` = expressiveness + stability, 4,000 chars per turn with error code `context_overflow`
- https://docs.bland.ai/api-v2/post/tts — HTTP body (`text` ≤ 5,000 chars, `voice`, `audio.{encoding,sample_rate,container}`, `controls`) and `x-model`/`x-cost`/`x-latency` response headers, unchanged
- https://docs.bland.ai/api-v2/get/tts-models — `btts-3` recommended, `btts-2` legacy; the voice picks the model
- https://docs.bland.ai/tts/realtime-concepts.md — server-side buffering, preemption and per-turn billing; confirms no client-side sentence threshold is wanted
- https://docs.bland.ai/llms.txt — page index for Bland Speech; no new TTS endpoint beyond v2 HTTP/WebSocket and the OpenAI-compatible `/v2/audio/speech`
- probe.py list-models / sdk-versions --provider bland — no catalogue and no SDK dependency for this provider
