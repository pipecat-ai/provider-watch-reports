---
service: simli/video
classes: [SimliVideoService]
date: 2026-09-14
pipecat_commit: f6d601303
default_model: null
summary: Nothing changed on either side in two weeks — same SDK, same service, same six standing gaps; none is urgent.
models_seen:
  - artalk
  - fasttalk
gaps:
  - item: "`enable_logging` is accepted but never used — simli-ai 2.x has no logging toggle"
    first_seen: 2026-08-31
    action: consider
    priority: medium
    note: the constructor and the deprecated `InputParams` both take `enable_logging`, and the docs page advertises it, but it reaches neither `SimliConfig` nor `SimliClient`; the SDK dropped its logging flag in the 2.0 overhaul and only exposes the stdlib logger `simli_client`
    needs: a call between deprecating the argument and honouring it by configuring the `simli_client` logger
  - item: Simli's avatar model (`SimliConfig.model`, fasttalk default or artalk) cannot be selected
    first_seen: 2026-08-31
    action: consider
    priority: medium
    note: the service hardwires `ServiceSettings(model=None)` and builds `SimliConfig` without a model, so artalk is unreachable; the SDK has offered the choice since simli-ai 0.1.14
    needs: whether `Settings.model` should map onto the `SimliModels` enum, and what a runtime settings update should do given `SimliConfig` is built once in `__init__`
  - item: Simli session events are never registered, so a session that dies mid-call is silent
    first_seen: 2026-09-01
    action: consider
    priority: medium
    note: "`SimliClient.registerEventCallback(SimliEvent.<...>, cb)` is documented for START, STOP, ERROR, SILENT, SPEAK and more; the service registers none, so an ERROR or STOP from Simli just ends the audio/video iterators and the avatar freezes with no `ErrorFrame` and no event handler for app code"
    needs: which events should become Pipecat event handlers versus an internal `push_error` on ERROR/STOP, and whether SPEAK/SILENT duplicate the TTS frames we already emit
  - item: "`handleSilence` is not reachable from the service"
    first_seen: 2026-08-31
    action: consider
    priority: low
    note: documented on both `SimliConfig` and the `/compose/token` request; with it off the avatar freezes instead of idling when the input buffer drains
    needs: the same construction-time question as the model setting — a `Settings` field would look live but only take effect at `__init__`
  - item: Transport and retry options are fixed
    first_seen: 2026-08-31
    action: consider
    priority: low
    note: "`enableSFU=True` is hard-coded and `transport_mode` (P2P or LIVEKIT), `retry_count` and `retry_timeout` are not exposed; only `simli_url` can be varied"
    needs: whether anyone wants the LiveKit transport from Pipecat, given Pipecat already owns the WebRTC leg
  - item: Simli's own Pipecat page documents a constructor signature we no longer have
    first_seen: 2026-08-31
    action: consider
    priority: low
    note: "https://docs.simli.com/api-reference/pipecat.md instantiates `SimliVideoService(SimliConfig(...))`; the current class takes `api_key` and `face_id`"
    needs: someone to file it with Simli — the page is in their docs, not ours
prs: []
error: null
---

# Simli video — 2026-09-14

No change since 2026-09-01: simli-ai is still 2.0.3, `src/pipecat/services/simli/video.py` is
untouched, and the six known gaps carry forward unchanged. Nothing is retiring.

## What's new for Pipecat

### PRs
Nothing.

### To consider
- **`enable_logging` does nothing** (medium, since 2026-08-31) — the constructor and the deprecated `InputParams` both take it and the docs page still advertises it, but it is never passed to `SimliConfig` or `SimliClient`. simli-ai 2.0.3 has no logging flag; the SDK logs through the stdlib logger named `simli_client` (`simli/logger.py`). — *Needs a call: deprecate the argument, or honour it by configuring that logger.*
- **Avatar model is not selectable** (medium, since 2026-08-31) — `SimliConfig.model` defaults to `SimliModels.fasttalk` and also accepts `artalk`; the service builds `SimliConfig` with only `faceId`, `maxSessionLength` and `maxIdleTime` and pins `ServiceSettings(model=None)`, so artalk is unreachable. — *Needs a call: whether `Settings.model` maps onto the enum, and what a runtime settings update means when the config is built once in `__init__`.*
- **Session events are not wired up** (medium, since 2026-09-01) — `registerEventCallback` is documented for START, STOP, ERROR, SILENT, SPEAK and more; `SimliVideoService` registers none. Errors are caught only around `start()` and the `send`/`clearBuffer` calls, so a session Simli ends or errors mid-call just terminates the two stream iterators: video stops, nothing goes upstream, no handler for app code. — *Needs a call: which events become Pipecat event handlers versus an internal `push_error` on ERROR/STOP, and whether SPEAK/SILENT add anything over the TTS frames the service already pushes.*
- **`handleSilence` is not reachable** (low, since 2026-08-31) — documented on `SimliConfig` and in the `/compose/token` body; off, the avatar freezes rather than idling when the buffer drains. — *Needs a call: same construction-time problem as the model setting.*
- **Transport and retry options are fixed** (low, since 2026-08-31) — `enableSFU=True` is hard-coded; `transport_mode` (P2P/LIVEKIT), `retry_count` and `retry_timeout` are not exposed. — *Needs a call: whether the LiveKit transport is wanted at all from Pipecat, which already owns the WebRTC leg.*
- **Simli's Pipecat page is stale** (low, since 2026-08-31) — it still shows `SimliVideoService(SimliConfig(...))`, which the current constructor rejects; the repo's own `examples/video-avatar/video-avatar-simli-video-service.py` uses the correct `api_key`/`face_id` form. — *Needs someone to file it with Simli; the page is in their docs.*

## Verification

| model | class             | ok | latency | note                                                              |
| ----- | ----------------- | -- | ------- | ----------------------------------------------------------------- |
| —     | SimliVideoService | —  | —       | `probe.py run` exits 3: "SimliVideoService is a video service: research-only, no probe" |

No live session was opened: nothing reached the PR bar, so a paid Simli session would have
bought nothing. Every SDK method the service calls is present in the installed simli-ai 2.0.3.

## Sources
- probe.py sdk-versions — simli-ai pin `~=2.0.1`, latest on PyPI still 2.0.3 (2026-06-16); PyPI release list shows no release after it
- probe.py list-models — no catalogue for simli; the model list is the SDK's `SimliModels` enum (fasttalk, artalk), unchanged
- simli-ai 2.0.3 `simli/simli.py` — `SimliConfig(faceId, handleSilence=True, maxSessionLength=600, maxIdleTime=30, model=SimliModels.fasttalk)`; `SimliClient(..., retry_count, retry_timeout, enableSFU, transport_mode)`; no logging flag
- simli-ai 2.0.3 `simli/events.py` / `simli/logger.py` — `SimliEvent`: START, STOP, ERROR, SILENT, SPEAK, CONNECTION_INFO, VIDEO_META, DESTINATION, ACK, UNKNOWN; logging is the stdlib logger `simli_client`
- https://docs.simli.com/api-reference/python.md — same config/client surface plus the `registerEventCallback` list; still documents no model field and no logging option
- https://docs.simli.com/api-reference/api_migration_guide.md — v2 renames only; the one removal (`POST /textToVideoStream`) and the deprecated Simli Auto endpoints are paths Pipecat does not call, and no removal date is given
- https://github.com/simliai/simli-client-py commit log — newest commit is still 2.0.3 "fix: height and width were flipped" (2026-06-16)
- https://docs.simli.com/api-reference/pipecat.md — Simli's integration page, still on the old `SimliConfig` constructor
- https://docs.pipecat.ai/api-reference/server/services/video/simli — what we document today, `enable_logging` included
- `gh pr list --label provider-watch --search simli` — no provider-watch PRs, open or closed, for this unit
