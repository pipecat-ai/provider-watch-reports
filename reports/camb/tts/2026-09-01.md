---
service: camb/tts
classes: [CambTTSService]
date: 2026-09-01
pipecat_commit: 3f9756c1e
default_model: mars-flash
summary: Nothing changed at Camb since the last run; mars-flash is still the right default, and CAMB_API_KEY is still rejected so the same five gaps stay unverified.
models_seen:
  - mars-8.1-flash-beta
  - mars-8.1-pro-beta
  - mars-flash
  - mars-instruct
  - mars-nano
  - mars-pro
gaps:
  - item: MARS 8.1 beta models are missing from MODEL_SAMPLE_RATES, so their 48 kHz audio is labelled 22050 Hz
    first_seen: 2026-08-31
    action: consider
    priority: medium
    note: "`mars-8.1-flash-beta` and `mars-8.1-pro-beta` are in the SDK's speech_model enum and are the API's documented default; `MODEL_SAMPLE_RATES.get(model, 22050)` falls back to 22050 for both, and the docs list both at 48 kHz. Camb also lists mars-flash as 22.05 kHz/48 kHz, which the single table entry cannot express."
    needs: a live probe to confirm the beta models' actual stream rate, which CAMB_API_KEY currently blocks; also a call on whether beta models belong in the table at all
  - item: Camb's `voice_settings` (speaking_rate, maintain_source_accent, enhance_reference_audio_quality) is not reachable from Settings
    first_seen: 2026-08-31
    action: consider
    priority: medium
    note: "`StreamTtsVoiceSettings` is a documented parameter of the streaming TTS endpoint the service calls; `speaking_rate` (default 1) is the speed control users usually ask for. `run_tts` never builds the object."
    needs: a probe with the field set, which the rejected credential blocks; and a call on whether to expose speaking_rate alone or the whole nested object
  - item: The constructor's `sample_rate` is never sent to Camb, so a user-chosen rate mislabels the audio
    first_seen: 2026-08-31
    action: consider
    priority: medium
    note: "`output_configuration` carries a documented `sample_rate` field, but the service only sets `format=\"pcm_s16le\"`. A caller passing `sample_rate=` gets a warning and frames tagged at their rate while the stream stays at the model's native rate."
    needs: a call on whether to request the rate from Camb or keep the warning-only behaviour, plus a live probe of a non-native rate
  - item: Camb's `inference_options` (stability, temperature, inference_steps, speaker_similarity) is not reachable from Settings
    first_seen: 2026-08-31
    action: consider
    priority: low
    note: MARS-8-family-only tuning knobs, documented as unsupported on the two 8.1 beta models; present in the SDK but absent from the endpoint's OpenAPI request payload.
    needs: a stance on per-model applicability — the fields silently do nothing on the beta models
  - item: "`enhance_named_entities_pronunciation` is not exposed"
    first_seen: 2026-08-31
    action: consider
    priority: low
    note: Top-level boolean on the streaming TTS request, default false; a one-field pass-through.
    needs: a live probe with the field set, which the rejected credential blocks
prs: []
error: CAMB_API_KEY is rejected by Camb.ai (401, "Invalid/Expired API Key"), so no probe could run and no change could be verified.
---

# Camb TTS — 2026-09-01

No provider-side change since 2026-08-31; `mars-flash` remains GA and the correct default, and the credential is still dead, so the same five gaps carry forward unverified and no branch was left.

## What's new for Pipecat

### PRs
Nothing.

### To consider
- **MARS 8.1 beta sample rates** (medium, since 2026-08-31) — `mars-8.1-flash-beta` and `mars-8.1-pro-beta` are in the SDK's `speech_model` enum and are the streaming endpoint's documented default, but `MODEL_SAMPLE_RATES` knows only the three GA models, so both betas fall back to 22050 Hz against Camb's documented 48 kHz — audio labelled at less than half its real rate. Two table entries would do it. Camb also lists `mars-flash` as "22.05kHz/48kHz", which one entry cannot express. — *Needs a call: a live probe must confirm the betas' actual stream rate (blocked), and whether beta models belong in the table at all.*
- **`voice_settings` pass-through** (medium, since 2026-08-31) — the endpoint takes `voice_settings` with `speaking_rate` (default 1), `maintain_source_accent` and `enhance_reference_audio_quality`; `run_tts` never builds the object, so there is no speed control. — *Needs a call: whether to expose `speaking_rate` alone or the whole nested object, and a probe with it set (blocked).*
- **Requested sample rate never reaches Camb** (medium, since 2026-08-31) — `output_configuration` has a documented `sample_rate` field; the service sets only `format="pcm_s16le"` and, when the caller passes `sample_rate=`, logs a warning while still tagging frames at the caller's rate over a stream at the model's native rate. — *Needs a call: request the rate from Camb, or keep warning-only.*
- **`inference_options` pass-through** (low, since 2026-08-31) — `stability`, `temperature`, `inference_steps`, `speaker_similarity` and friends are unreachable; documented as MARS-8-family-only and unsupported on the two 8.1 betas, and the endpoint's OpenAPI payload does not list the object at all. — *Needs a call: what to do when the selected model ignores them.*
- **`enhance_named_entities_pronunciation`** (low, since 2026-08-31) — top-level boolean, default false, not exposed. — *Needs a call: a probe with the field set, which the rejected credential blocks.*

## Verification

| model      | class          | ok | latency | note                             |
| ---------- | -------------- | -- | ------- | -------------------------------- |
| mars-flash | CambTTSService | ❌ | —       | HTTP 401 Invalid/Expired API Key |

The failure is at authentication, not at the model name; the run says nothing about model availability. The betas were not probed a second time — the credential fails before any model name is evaluated.

## Sources
- https://docs.camb.ai/models — unchanged since 2026-08-31: `mars-flash` (GA, ~150 ms TTFB, 22.05kHz/48kHz), `mars-pro` (GA, 48 kHz), `mars-instruct` (GA, 22.05 kHz, non-realtime), `mars-8.1-flash-beta` / `mars-8.1-pro-beta` (beta, 48 kHz), `mars-nano` (partner-deployed). No deprecations or retirements.
- https://docs.camb.ai/api-reference/endpoint/create-tts-stream — request schema, unchanged: `speech_model` defaults to `mars-8.1-flash-beta`; `enhance_named_entities_pronunciation` (false); nested `output_configuration` (`format` default wav, `sample_rate`, `apply_enhancement`) and `voice_settings` (`speaking_rate` 1, `maintain_source_accent`, `enhance_reference_audio_quality`). The OpenAPI payload does not list `inference_options`, though the prose does.
- probe.py sdk-versions — camb-sdk pin `>=1.5.4,<2`, latest 1.6.0 (2026-08-27), inside the pin; 1.5.17 installed.
- camb-sdk 1.6.0 wheel vs installed 1.5.17 — `text_to_speech/client.py`, `StreamTtsVoiceSettings` and the `speech_model` literal are byte-identical, so the newer release adds nothing this service could use. `StreamTtsVoiceSettings` also carries an undocumented `apply_ref_loudness_norm` (default False).
- probe.py run --service CambTTSService --model mars-flash — HTTP 401 Invalid/Expired API Key; no live verification possible.
- probe.py list-models --provider camb — no catalogue endpoint; model list taken from the docs.
- WebSearch "Camb.ai changelog" — no changelog page exists; the MARS8 launch post (2026-01-20) is still the most recent product announcement.
