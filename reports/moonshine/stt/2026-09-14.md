---
service: moonshine/stt
classes:
- MoonshineSTTService
date: 2026-09-14
pipecat_commit: f6d601303
default_model: small-streaming
summary: Moonshine relicensed its models MIT in every language; the service docstring still calls the non-English ones non-commercial — branch ready. No SDK, catalogue or model change since the last run.
models_seen:
- base
- medium-streaming
- small-streaming
- tiny
- tiny-streaming
gaps:
- item: Service docstring calls all non-English models non-commercial, but Moonshine relicensed everything MIT except the legacy non-streaming non-English models
  first_seen: 2026-09-14
  action: pr
- item: Keyterm biasing (`set_keyterms` / `set_context`) is not reachable from Settings
  first_seen: 2026-09-01
  action: consider
  priority: medium
  note: moonshine-voice 0.1.5 `Transcriber.set_keyterms(list)` and `set_context(text, max_terms)` bias decoding toward supplied terms; neither is called, and `keyterm_boost` sets the strength at load time
  needs: both raise on non-streaming architectures (`tiny`, `base`), so exposing them needs a stance on what happens when the configured model cannot bias
- item: Alphanumeric spelling mode is never enabled although its model is downloaded
  first_seen: 2026-09-01
  action: consider
  priority: medium
  note: '`get_model_for_language` pre-fetches the language''s `spelling_cnn.ort` best-effort, but `Transcriber` is built without `spelling_model_path` and `transcribe_without_streaming` runs with the default `flags=0`, so `MOONSHINE_FLAG_SPELLING_MODE` never applies'
  needs: whether spelling fusion should be a Settings flag or on by default, and whether it costs latency on the CPU path
- item: Transcription is batch-only; Moonshine's streaming API and its partial results are unused
  first_seen: 2026-09-01
  action: consider
  priority: medium
  note: the service is a `SegmentedSTTService` calling `transcribe_without_streaming` once per VAD segment; `Transcriber.add_audio` plus a listener emits interim text at `update_interval`
  needs: a decision on whether Moonshine gets a second, streaming service class alongside the segmented one
- item: Speaker diarization (`identify_speakers`) is not reachable from Settings
  first_seen: 2026-09-14
  action: consider
  priority: low
  note: 0.1.5 diarizes locally via the `identify_speakers` transcriber option plus `diarization_model_dir` from `get_diarization_model()` (an 8.2 MB download), filling `TranscriptLine.speaker_spans`; per-VAD-segment transcription makes it near-useless for a single-user turn, so it matters only for multi-speaker audio
  needs: '`TranscriptionFrame` cannot carry speaker spans, so surfacing them needs a stance like Speechmatics'' speaker-label formatting'
- item: Word timestamps are not requested or surfaced
  first_seen: 2026-09-01
  action: consider
  priority: low
  note: '`get_model_for_language(..., include_word_timestamps=True)` plus the `word_timestamps` transcriber option fills `TranscriptLine.words`; the service asks for neither'
  needs: Pipecat's `TranscriptionFrame` has nowhere to carry word timings, so this needs a frame-level decision first
- item: moonshine-voice ships on-device TTS with no Pipecat service to reach it
  first_seen: 2026-09-01
  action: consider
  priority: low
  note: 0.1.5 exposes `list_tts_voices`, `get_tts_voice_catalog`, voice cloning and a streaming synthesizer; Pipecat has no `MoonshineTTSService`, so nothing in the inventory watches it
  needs: whether a local Moonshine TTS service is wanted next to Kokoro
prs:
- branch: provider-watch/moonshine-stt-license
  state: open
  summary: Correct the Moonshine model licensing note
  url: https://github.com/pipecat-ai/pipecat/pull/5745
  opened: '2026-09-14'
error: null
---

# Moonshine STT — 2026-09-14

Moonshine made its models MIT by default in every language and size (0.1.5,
2026-08-24); the service docstring — which generates the docs page — still tells
users every non-English model is non-commercial. Branch ready. Nothing else moved:
moonshine-voice is still 0.1.5 and the catalogue is byte-identical to the last run.
`small-streaming` stays the right default.

## What's new for Pipecat

### PRs
- https://github.com/pipecat-ai/pipecat/pull/5745 — Correct the Moonshine model licensing note

### To consider
- **Keyterm biasing** (medium, since 2026-09-01) — `Transcriber.set_keyterms(["Kubernetes", ...])` and `set_context(passage, max_terms)` bias decoding toward domain terms between segments, with `keyterm_boost` for strength; the service calls neither. — *Needs a call: both raise `MoonshineError` on non-streaming architectures (`tiny`, `base`), so exposing them needs a stance on what happens when the configured model cannot bias.*
- **Alphanumeric spelling mode** (medium, since 2026-09-01) — `get_model_for_language` already best-effort downloads the language's `spelling_cnn.ort` on every run, but `Transcriber` is constructed without `spelling_model_path` and transcribe calls use the default `flags=0`, so the digit/letter fusion is dead weight. — *Needs a call: whether spelling fusion should be a Settings flag or on by default, and whether it costs latency on the CPU path.*
- **Streaming partials** (medium, since 2026-09-01) — `SegmentedSTTService` transcribes each VAD segment in one `transcribe_without_streaming` call; Moonshine's own `add_audio` plus listener path emits interim text every `update_interval` and would cut the wait after end-of-turn. — *Needs a call: whether Moonshine gets a second, streaming service class alongside the segmented one.*
- **Speaker diarization** (low, since 2026-09-14) — the `identify_speakers` option plus `diarization_model_dir` from `get_diarization_model()` clusters speakers locally and fills `TranscriptLine.speaker_spans`; Deepgram and Speechmatics expose the equivalent in Pipecat, Moonshine does not. Each VAD segment is one speaker by construction, so this only pays off on multi-speaker audio. — *Needs a call: `TranscriptionFrame` cannot carry speaker spans, so surfacing them needs a stance like Speechmatics' speaker-label formatting, plus an opt-in for the 8.2 MB model download.*
- **Word timestamps** (low, since 2026-09-01) — `get_model_for_language(..., include_word_timestamps=True)` fetches the attention decoder the `word_timestamps` option needs to fill `TranscriptLine.words`; neither is requested. — *Needs a call: `TranscriptionFrame` has nowhere to carry word timings, so this needs a frame-level decision first.*
- **No Moonshine TTS service** (low, since 2026-09-01) — the same package ships on-device TTS with a voice catalog, voice cloning and a streaming synthesizer; Pipecat exposes only the STT half. — *Needs a call: whether a local Moonshine TTS service is wanted next to Kokoro.*

The previous run's German/Tagalog gap is closed: pipecat-ai/pipecat#5557 merged on
2026-09-02 and the ten catalogue languages are all in `LANGUAGE_MAP`.

English's catalogue default is `medium-streaming`, not the service's
`small-streaming`, but medium is 23% slower on this CPU (below), so the default
stands and this is not a gap. `Model.BASE_STREAMING` remains in the enum and in no
language's catalogue; the fallback to the language's best model covers it.

Note for the next run: moonshine-voice still prints "Using a model released under
the non-commercial Moonshine Community License" on stderr for *any* non-English
language inside `get_model_for_language`, which its own LICENSE now contradicts for
streaming models. Upstream text we cannot fix from Pipecat.

## Verification

| model            | class               | ok | latency                  | note                                                        |
| ---------------- | ------------------- | -- | ------------------------ | ----------------------------------------------------------- |
| small-streaming  | MoonshineSTTService | ✅ | TTFB 1853 ms (median of 5) | default; spread 1788–1920 ms; bundled clip transcribed       |
| medium-streaming | MoonshineSTTService | ✅ | TTFB 2279 ms (median of 5) | spread 2244–2332 ms; English's catalogue default, 23% slower |

The branch changes only a docstring, so these runs (against the same code path)
stand as its verification.

## Sources
- `probe.py sdk-versions --provider moonshine` — moonshine-voice 0.1.5 (2026-08-24) is still the latest release; the pin `>=0.1.5` is at it
- `probe.py list-models --provider moonshine` — no catalogue wired up (models are local); the native catalogue below is the source
- `moonshine_voice.download._stt_catalog()` (0.1.5) — the published catalogue, unchanged from the 2026-09-01 run: ar (tiny-streaming*, base), de (small-streaming*, tiny-streaming), en (medium-streaming*, small-streaming, base, tiny-streaming, tiny), es (small-streaming*, tiny-streaming, base), ja (small-streaming*, tiny-streaming, base, tiny), ko (tiny*), tl (tiny-streaming*), uk (base*), vi (tiny-streaming*, base), zh (tiny-streaming*, base) — `*` marks the language's default. It is compiled into `libmoonshine.so`, so it cannot drift without an SDK release
- https://github.com/moonshine-ai/moonshine/blob/main/LICENSE (commit 547d0066, in v0.1.5) — models are MIT by default in every language and size; the exhaustive non-commercial list is the legacy non-streaming models for ar, ja, ko, zh, uk, vi (Base + Tiny) and es (Base)
- https://github.com/moonshine-ai/moonshine `CHANGELOGS.md` / `docs/models/available-models.md` — same relicensing, plus Japanese Tiny added to the model table
- moonshine-voice 0.1.5 `transcriber.py`, `moonshine_api.py`, `download.py`, `mic_transcriber.py` — `set_keyterms`/`set_context` and their streaming-only restriction, `spelling_model_path` + `MOONSHINE_FLAG_SPELLING_MODE`, `identify_speakers` + `diarization_model_dir` + `SpeakerSpan`, `word_timestamps`, and the best-effort spelling prefetch inside `get_model_for_language`
- https://github.com/moonshine-ai/moonshine/releases and https://pypi.org/project/moonshine-voice/ — v0.1.5 (2026-08-24) is the newest of eight releases since 2026-07-16; nothing in three weeks
- https://docs.pipecat.ai/api-reference/server/services/stt/moonshine — generated from the service docstrings, so it carries the stale licensing sentence the branch corrects
- pipecat-ai/pipecat#5557 — merged 2026-09-02, closing the German/Tagalog language-map gap
