---
service: speechmatics/tts
classes:
- SpeechmaticsTTSService
date: 2026-08-31
pipecat_commit: 6c94c365d
default_model: null
summary: Speechmatics TTS is still the same preview API (text + voice + output_format) and Pipecat covers all of it; one branch fixes the version string we report to Speechmatics.
models_seen:
- jack
- megan
- sarah
- theo
gaps:
- item: '`sm-app` reports the speechmatics-rt SDK version under the `pipecat/` label'
  first_seen: 2026-08-31
  action: pr
- item: A sample rate other than 16 kHz makes every request 422 with an opaque error
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: the API only serves `pcm_16000`/`wav_16000`, but `_get_endpoint_url` builds `output_format=pcm_{sample_rate}`; with `sample_rate=None` (adopt the pipeline rate, commonly 24 kHz) the probe got `Service unavailable [422]` and no audio, after only a warning at construction
  needs: whether the service should pin 16 kHz regardless of the caller's sample rate or raise at construction — either is a behavior change
- item: Service requires a caller-supplied `aiohttp_session`
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: alone among Pipecat's HTTP TTS services; probe.py cannot construct it, so this unit needs an ad-hoc probe every run
  needs: making the session optional means owning its lifecycle in the service — a constructor change the team may not want
- item: TTS is still a free preview on `preview.tts.speechmatics.com`
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: Speechmatics' quickstart still says "currently in preview and free to use. Features and pricing may change"; the hard-coded `base_url` default will need revisiting at GA
  needs: nothing to do until Speechmatics announces GA and a stable hostname — re-check each run
- item: Speechmatics' own Pipecat TTS page documents the deprecated `voice_id=` argument
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: docs.speechmatics.com/integrations-and-sdks/pipecat/tts shows `voice_id="sarah"` instead of `settings=SpeechmaticsTTSService.Settings(voice=...)`
  needs: the page lives in Speechmatics' docs repo; someone has to raise it with them
prs:
- branch: provider-watch/speechmatics-tts-sm-app-version
  state: branch
  summary: Report Pipecat's version in the Speechmatics TTS sm-app tag
  capped: true
error: null
---

# Speechmatics TTS — 2026-08-31

The provider hasn't moved: the preview API still takes only `text`, a voice in the path and
`output_format`, and Pipecat reaches all of it. One branch fixes the version we self-report.

## What's new for Pipecat

### PRs
- `provider-watch/speechmatics-tts-sm-app-version` — review: `git show provider-watch/speechmatics-tts-sm-app-version` — Report Pipecat's version in the Speechmatics TTS sm-app tag

### To consider
- **Non-16 kHz sample rate fails every request** (medium, since 2026-08-31) — the API serves only `pcm_16000`/`wav_16000`, but the URL builder emits `output_format=pcm_{sample_rate}`. With `sample_rate=None` the service adopts the pipeline's output rate (24 kHz by default) and every synthesis returns 422 with `Service unavailable [422]`; construction only logs "may cause issues". — *Needs a call: pin 16 kHz regardless of the caller's sample rate, or raise at construction — either changes behavior.*
- **Caller must supply an `aiohttp_session`** (low, since 2026-08-31) — the only Pipecat HTTP TTS service that requires one; `probe.py` cannot construct it, so this unit needs an ad-hoc probe each run. — *Needs a call: making the session optional means the service owns its lifecycle, a constructor change.*
- **Still a preview endpoint** (low, since 2026-08-31) — `base_url` defaults to `https://preview.tts.speechmatics.com`, which Speechmatics documents as preview and free with pricing and features subject to change. — *Needs a call: nothing to do until GA announces a stable hostname; re-check each run.*
- **Speechmatics' Pipecat page is out of date** (low, since 2026-08-31) — their integration doc still shows the deprecated `voice_id=` argument. — *Needs a call: the page is in Speechmatics' docs repo; someone has to raise it with them.*

## Verification

| voice/config           | class                   | ok | latency                     | note |
| ---------------------- | ----------------------- | -- | --------------------------- | ---- |
| sarah (default)        | SpeechmaticsTTSService  | ✅ | TTFB 372 ms (median of 3)   | 7 audio frames |
| theo                   | SpeechmaticsTTSService  | ✅ | TTFB 824 ms                 |      |
| megan                  | SpeechmaticsTTSService  | ✅ | TTFB 847 ms                 |      |
| jack                   | SpeechmaticsTTSService  | ✅ | TTFB 846 ms                 | absent from the `speechmatics-tts` SDK enum, works anyway |
| sarah, `sample_rate=None` | SpeechmaticsTTSService | ❌ | —                        | `Service unavailable [422]`, no audio |
| sarah, branch applied  | SpeechmaticsTTSService  | ✅ | TTFB 609 ms                 | corrected `sm-app` tag accepted |

`probe.py run` cannot build this class (`missing 1 required keyword-only argument: 'aiohttp_session'`),
so the rows above come from an ad-hoc script reusing `probe.py`'s pipeline runner.

## Sources
- https://docs.speechmatics.com/text-to-speech/quickstart — the whole TTS API: POST `/generate/<voice>` with `{"text": ...}`, `output_format` of `wav_16000` (default) or `pcm_16000`, four voices (sarah, theo, megan, jack), English only, no speed/pitch/emphasis control, still preview and free
- https://docs.speechmatics.com/sitemap.xml — one TTS page only; nothing new under text-to-speech, and the private early-access pages (Melia 1, next-gen model, Voice Agent API) are STT/agent, not TTS
- https://docs.speechmatics.com/integrations-and-sdks/pipecat/tts — Speechmatics' own Pipecat TTS instructions, still using `voice_id=`
- https://pypi.org/project/speechmatics-tts/ 0.1.2 (2025-11-11) — the official TTS SDK; `generate()` sends only `{"text"}` to `/generate/{voice}?output_format=...`, confirming Pipecat's hand-rolled request matches the full API surface. Its `Voice` enum lists only sarah/theo/megan
- probe.py sdk-versions — `speechmatics-voice[smart]~=0.2.8`, latest stable 0.2.8 (2026-01-26); 0.3.0bN prereleases exist but the TTS path uses `speechmatics.rt` only for the install check
- probe.py list-models — exit "no model catalogue known"; Speechmatics TTS has no models
- src/pipecat/services/speechmatics/stt.py:674 — passes `app=f"pipecat/{pipecat_version()}"`, the identifier the TTS branch aligns with
