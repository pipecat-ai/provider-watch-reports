---
service: speechmatics/tts
classes:
- SpeechmaticsTTSService
date: 2026-09-14
pipecat_commit: f6d601303
default_model: null
summary: Speechmatics TTS is unchanged — same preview API, same four voices — and Pipecat covers all of it; last run's sm-app branch merged, and the only new item is that the TTS install guard now imports a transitive dependency after the extra moved to speechmatics-agent-stt.
models_seen:
- jack
- megan
- sarah
- theo
gaps:
- item: A sample rate other than 16 kHz makes every request 422 with an opaque error
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: the API only serves `pcm_16000`/`wav_16000`, but `_get_endpoint_url` builds `output_format=pcm_{sample_rate}`; with `sample_rate=None` (adopt the pipeline rate, commonly 24 kHz) this run's probe again got `Service unavailable [422]` and no audio, after only a warning at construction
  needs: whether the service should pin 16 kHz regardless of the caller's sample rate or raise at construction — either is a behavior change
- item: TTS install check imports `speechmatics.rt`, which the `speechmatics` extra no longer depends on directly
  first_seen: 2026-09-14
  action: consider
  priority: low
  note: the extra is now `speechmatics-agent-stt~=0.1.0` (was `speechmatics-voice[smart]~=0.2.8`) and `stt.py` imports `speechmatics.agent_stt`; `tts.py:32` still guards on `speechmatics.rt`, which resolves only because `speechmatics-agent-stt` requires `speechmatics-rt>=1.1.1`. The TTS path is pure aiohttp and uses neither package
  needs: whether the guard should follow the extra to `speechmatics.agent_stt` or be dropped, since TTS imports no Speechmatics SDK at all
- item: Service requires a caller-supplied `aiohttp_session`
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: alone among Pipecat's HTTP TTS services; probe.py cannot construct it, so this unit needs an ad-hoc probe every run
  needs: making the session optional means owning its lifecycle in the service — a constructor change the team may not want
- item: TTS is still a free preview on `preview.tts.speechmatics.com`
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: the quickstart still says "currently in preview and free to use" and still carries the un-updated 2025 pricing dates; the marketing pricing page now lists TTS under the Free/Pro/Enterprise tiers without a rate, but no GA hostname is documented
  needs: nothing to do until Speechmatics announces GA and a stable hostname — re-check each run
- item: Speechmatics' own Pipecat TTS page documents the deprecated `voice_id=` argument
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: docs.speechmatics.com/integrations-and-sdks/pipecat/tts still shows `voice_id="sarah"` instead of `settings=SpeechmaticsTTSService.Settings(voice=...)`
  needs: the page lives in Speechmatics' docs repo; someone has to raise it with them
prs: []
error: null
---

# Speechmatics TTS — 2026-09-14

Nothing moved on the provider side: the preview API still takes only `text`, a voice in the
path and `output_format`, and Pipecat reaches all of it. No branch this run — last run's
`sm-app` fix merged as #5569.

## What's new for Pipecat

### PRs
Nothing.

### To consider
- **Non-16 kHz sample rate fails every request** (medium, since 2026-08-31) — the API serves only `pcm_16000`/`wav_16000`, but the URL builder emits `output_format=pcm_{sample_rate}`. With `sample_rate=None` the service adopts the pipeline's output rate (24 kHz by default) and every synthesis returns 422 with `Service unavailable [422]` and no audio; construction only logs "may cause issues". — *Needs a call: pin 16 kHz regardless of the caller's sample rate, or raise at construction — either changes behavior.*
- **Install guard points at a package the extra dropped** (low, since 2026-09-14) — the `speechmatics` extra is now `speechmatics-agent-stt~=0.1.0` and `stt.py` imports `speechmatics.agent_stt`, but `tts.py` still does `import speechmatics.rt` as its "did you install the extra?" check. That resolves today only because `speechmatics-agent-stt` requires `speechmatics-rt>=1.1.1`; the TTS path itself is pure aiohttp. — *Needs a call: point the guard at `speechmatics.agent_stt` to match the extra, or drop it since TTS imports no Speechmatics SDK.*
- **Caller must supply an `aiohttp_session`** (low, since 2026-08-31) — the only Pipecat HTTP TTS service that requires one; `probe.py` cannot construct it, so this unit needs an ad-hoc probe each run. — *Needs a call: making the session optional means the service owns its lifecycle, a constructor change.*
- **Still a preview endpoint** (low, since 2026-08-31) — `base_url` defaults to `https://preview.tts.speechmatics.com`, documented as preview and free with pricing and features subject to change. The marketing pricing page now lists TTS in all three tiers but quotes no rate and names no GA host. — *Needs a call: nothing to do until GA announces a stable hostname; re-check each run.*
- **Speechmatics' Pipecat page is out of date** (low, since 2026-08-31) — their integration doc still shows the deprecated `voice_id=` argument. — *Needs a call: the page is in Speechmatics' docs repo; someone has to raise it with them.*

## Verification

| voice/config              | class                  | ok | latency                   | note |
| ------------------------- | ---------------------- | -- | ------------------------- | ---- |
| sarah (default), 16 kHz   | SpeechmaticsTTSService | ✅ | TTFB 477 ms (median of 3) | spread 472–512 ms, 4 audio frames each |
| sarah, `sample_rate=None` | SpeechmaticsTTSService | ❌ | —                         | `Service unavailable [422]`, no audio |

`probe.py run` cannot build this class (keyword-only `aiohttp_session`), so both rows come
from `.pw-scratch/speechmatics_tts_probe.py`, which reuses `probe.py`'s pipeline runner and
metrics. The other three voices (theo, megan, jack) were confirmed working on 2026-08-31 and
the catalogue is unchanged, so they were not re-billed.

## Sources
- https://docs.speechmatics.com/text-to-speech/quickstart — the whole TTS API, unchanged: POST `/generate/<voice>` with `{"text": ...}`, `output_format` of `wav_16000` (default) or `pcm_16000`, the same four voices (sarah, theo, megan, jack), English only, no speed/pitch/emphasis controls, still preview and free with the stale 2025 pricing dates
- https://docs.speechmatics.com/sitemap.xml — still exactly one TTS page; nothing added under text-to-speech
- https://speechmatics.featurebase.app/changelog — entries through 2026-09-11 (Melia 1 accuracy, AU realtime endpoint `au.rt.speechmatics.com`, ForceEndOfUtterance truncation fix, GPU Enhanced English) are all STT/batch; no TTS entry
- https://docs.speechmatics.com/integrations-and-sdks/pipecat/tts — Speechmatics' own Pipecat TTS instructions, still using `voice_id=`
- https://www.speechmatics.com/pricing — TTS now appears as a line item in the Free/Pro/Enterprise tiers, but with no rate and no GA hostname
- https://docs.pipecat.ai/api-reference/server/services/tts/speechmatics — our page is current: `Settings(voice=...)` example, `voice_id` marked deprecated, notes the 16 kHz-only output
- probe.py sdk-versions — the `speechmatics` extra is now `speechmatics-agent-stt~=0.1.0`, latest 0.1.0 (2026-09-07), at the pin
- probe.py list-models — "no model catalogue known"; Speechmatics TTS has no models, only voices
- pyproject.toml:143 and src/pipecat/services/speechmatics/tts.py:32 — the extra's package versus the `speechmatics.rt` guard the TTS service still uses
- https://github.com/pipecat-ai/pipecat/pull/5569 — merged 2026-09-02; the `sm-app` version tag now reports `pipecat/<version>`, closing last run's gap
