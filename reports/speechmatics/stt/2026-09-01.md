---
service: speechmatics/stt
classes:
- SpeechmaticsSTTService
date: 2026-09-01
pipecat_commit: 531ec8960
default_model: enhanced
summary: Setting `split_sentences` still crashes the service; the branch fixes that and exposes `include_results`.
models_seen:
- enhanced
- melia-1
- standard
gaps:
- item: Setting `split_sentences` raises ValueError and fails service construction
  first_seen: 2026-08-31
  action: pr
- item: Speechmatics `include_results` (word-level results) is not reachable from Settings
  first_seen: 2026-08-31
  action: pr
- item: '`operating_point` is deprecated in favour of `model`, and every session logs the SDK''s deprecation warning'
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: 'Speechmatics'' Models page says `operating_point` is deprecated in SaaS and maps to `model`; speechmatics-voice 0.3.0b8 drops it from `VoiceAgentConfig` for `model: Model`, so `Settings.operating_point` and `default_settings.model = self._config.operating_point.value` both need migrating with the pin bump'
  needs: whether to wait for 0.3.0 GA (0.2.8 is still the latest stable) or add a `model` alias against the 0.2.x field now
- item: Default base URL is `wss://eu2.rt.speechmatics.com/v2`, not the documented global endpoint
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: '`global.rt.speechmatics.com` has been the all-regions endpoint since 2026-07-20 and routes to the nearest region; `eu2` is an undocumented alias of `eu.rt.speechmatics.com`'
  needs: routing to the nearest region changes which region processes audio, so changing the default needs a stance on data residency
- item: '`speech_segment_config` is rebuilt unconditionally, so `add_trailing_eos` and `pause_mark` are unreachable'
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: '`_build_config` assigns a fresh `SpeechSegmentConfig(emit_sentences=...)` after the `extra_params` loop, discarding both the preset value and anything a user set via `extra_params`'
  needs: applying `emit_sentences` in place would let the SDK preset default (True) through and change behaviour for existing users, so this needs a decision on the default before the two other knobs can be exposed
- item: VAD, smart-turn and end-of-turn tuning are not Settings fields
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: '`vad_config` (enabled/silence_duration/threshold), `smart_turn_config` and `end_of_turn_config` shape the ADAPTIVE and SMART_TURN modes the service exposes as `TurnDetectionMode`, but are settable only through `extra_params`'
  needs: these are nested SDK models; exposing them needs a stance on whether Settings takes SDK types directly or mirrors them
- item: Language and output-locale maps are behind the provider's list
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: '`BASE_LANGUAGES` omits `tl`, `ia` and the bilingual packs `ar_en`, `en_ms`, `cmn_en`, `cmn_en_ms_ta`, `en_ta` (they still work, via `resolve_language`''s pass-through plus a ''not verified'' warning); `LOCALES` covers only en-GB/en-US/en-AU while the docs also list Mandarin and Portuguese output locales'
  needs: the bilingual pack codes have no `Language` enum member, so covering them means adding members or documenting the raw-string route
- item: Remaining VoiceAgentConfig options and the fast/captions/scribe presets are only reachable indirectly
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: '`enable_entities`, `max_delay_mode`, `transcription_update_preset`, `audio_buffer_length` and `advanced_engine_control` work through `extra_params`; `TurnDetectionMode` covers four of the SDK''s seven presets'
  needs: whether the `extra_params` escape hatch is considered sufficient for these, and whether non-agent presets belong in a turn-detection enum
prs:
- branch: provider-watch/speechmatics-stt-include-results
  state: open
  summary: Fix the split_sentences crash and expose Speechmatics word-level results
  url: https://github.com/pipecat-ai/pipecat/pull/5568
  opened: '2026-09-01'
error: null
---

# Speechmatics STT — 2026-09-01

Nothing changed provider-side since the last run; the branch that fixes the `split_sentences` crash and adds `include_results` was never published, so it is rebuilt here on current `main`.

## What's new for Pipecat

### PRs
- https://github.com/pipecat-ai/pipecat/pull/5568 — Fix the split_sentences crash and expose Speechmatics word-level results

### To consider
- **`operating_point` → `model`** (medium, since 2026-08-31) — Speechmatics' Models page deprecates `operating_point` in SaaS in favour of `model`, and every probe run logs `TranscriptionConfig.operating_point is deprecated ... Please use the model property instead.` speechmatics-voice 0.3.0b8 replaces the field with `model: Model`, which will break `default_settings.model = self._config.operating_point.value` and `Settings.operating_point`. — *Needs a call: whether to wait for 0.3.0 GA (0.2.8 is still the latest stable and matches our `~=0.2.8` pin) or alias `model` onto the 0.2.x field now.*
- **Global realtime endpoint** (medium, since 2026-08-31) — the documented Realtime SaaS endpoints are `global.rt.speechmatics.com`, `eu.rt.speechmatics.com` and `us.rt.speechmatics.com`; the service defaults to `wss://eu2.rt.speechmatics.com/v2`, an undocumented alias of the EU endpoint. The 2026-08-31 probe against the global endpoint transcribed fine. — *Needs a call: the global endpoint may route to any region, so changing the default needs a stance on data residency.*
- **`speech_segment_config` is clobbered** (medium, since 2026-08-31) — `_build_config` assigns a fresh `SpeechSegmentConfig(emit_sentences=...)` after the `extra_params` loop, so `add_trailing_eos` and `pause_mark` cannot be reached by any route. — *Needs a call: setting `emit_sentences` in place instead would let the SDK preset default (True) through, changing behaviour for existing users.*
- **Turn-detection sub-configs** (medium, since 2026-08-31) — `vad_config`, `smart_turn_config` and `end_of_turn_config` tune the very modes `TurnDetectionMode` selects, but are only settable via `extra_params`. — *Needs a call: they are nested SDK models, so exposing them needs a stance on whether Settings accepts SDK types directly.*
- **Language and locale maps** (low, since 2026-08-31) — `BASE_LANGUAGES` omits `tl`, `ia` and the five bilingual packs (each still works, with a "Language ... not verified" warning); `LOCALES` covers only the three English locales although the docs also document Mandarin and Portuguese output locales. — *Needs a call: bilingual pack codes have no `Language` enum member.*
- **Remaining config options and presets** (low, since 2026-08-31) — `enable_entities`, `max_delay_mode`, `transcription_update_preset`, `audio_buffer_length` and `advanced_engine_control` are reachable only through `extra_params`, and `TurnDetectionMode` exposes four of the SDK's seven presets (`fast`, `captions`, `scribe` are absent). — *Needs a call: whether `extra_params` is considered sufficient here.*

## Verification

| model    | class                  | ok | latency      | note                                                                |
| -------- | ---------------------- | -- | ------------ | ------------------------------------------------------------------- |
| enhanced | SpeechmaticsSTTService | ✅ | TTFB 1095 ms | current default on `main`                                            |
| enhanced | SpeechmaticsSTTService | ✅ | TTFB 871 ms  | branch, `--setting split_sentences=true`; raises ValueError on `main` |
| enhanced | SpeechmaticsSTTService | ✅ | TTFB 1125 ms | branch, `--setting include_results=true`, accepted by the API         |

Single samples, not a default-bump comparison.

## Sources
- https://speechmatics.featurebase.app/changelog — no entries after 2026-08-20 (ForceEndOfUtterance latency); nothing new since the last run, no deprecations announced
- https://docs.speechmatics.com/speech-to-text/models.md — enhanced / standard / melia-1; `operating_point` deprecated in SaaS and mapped to `model`; Melia 1 is batch-only
- https://docs.speechmatics.com/get-started/authentication.md — Realtime SaaS endpoints are `global.rt`, `eu.rt`, `us.rt`
- https://docs.speechmatics.com/voice-agents/voice-sdk.md — the Voice SDK config surface this service wraps
- probe.py sdk-versions — speechmatics-voice 0.2.8 (2026-01-26) is still the latest stable and matches our `~=0.2.8` pin; PyPI carries only 0.3.0b1–b8 prereleases, unchanged since 2026-07-29
- probe.py list-models --provider speechmatics — no catalogue; model list taken from the docs
- `VoiceAgentConfig.model_fields` (installed SDK) — has `include_results` but no `split_sentences`, which is what makes the pass-through loop raise
