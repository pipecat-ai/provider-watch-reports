---
service: aws/realtime
classes:
- AWSNovaSonicLLMService
date: 2026-08-31
pipecat_commit: 712d7643c
default_model: amazon.nova-2-sonic-v1:0
summary: Default Nova 2 Sonic is current; the first-generation model Pipecat still documents and code-paths reaches Bedrock EOL on 2026-09-14.
models_seen:
- amazon.nova-2-sonic-v1:0
- amazon.nova-sonic-v1:0
gaps:
- item: First-generation amazon.nova-sonic-v1:0 reaches Bedrock EOL on 2026-09-14, but the service still carries and documents a support path for it
  first_seen: 2026-08-31
  action: consider
  priority: high
  note: '`_is_first_generation_sonic_model()` gates the `ready.wav` assistant-response-trigger hack, `AWAIT_TRIGGER_ASSISTANT_RESPONSE_INSTRUCTION` and the `endpointing_sensitivity` downgrade warning; the docstring and three examples tell users how to run the older model. Bedrock lists it Legacy since 2026-03-13, EOL 2026-09-14, after which requests fail.'
  needs: a call on whether to deprecate-and-remove the first-generation code path (and `ready.wav`) for 2.0.0 or only strip the guidance from docstrings and examples
- item: aws_sdk_bedrock_runtime is pinned to ~=0.4.0 (2026-02-24) while 0.11.0 is current
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: seven minor releases behind; the package is pre-1.0 and its own PyPI description warns of breaking changes before 1.0, so the pin cannot simply be widened without reading each release
  needs: someone to walk 0.5.0 through 0.11.0 for breaking changes to invoke_model_with_bidirectional_stream and pick a new pin
- item: Nova 2 Sonic cross-modal text input is only sent at connect, not during a live session
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: the wire support exists — `send_text(..., interactive=True)` — but `_handle_context()` only uses it for the last message of the *initial* context; a later `LLMContextFrame` only flushes completed function-call results, so text typed mid-call never reaches the model
  needs: a decision on which frame carries mid-session text (a new `LLMContextFrame` user message vs. an explicit append frame) and how it should interact with an in-flight assistant turn
- item: Nova 2 Sonic supported-region list in the docstring and realtime example omits eu-north-1
  first_seen: 2026-08-31
  action: pr
- item: The SYSTEM_SPEECH content role is missing from the Nova Sonic adapter's Role enum
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: Nova 2 Sonic accepts SYSTEM_SPEECH as the `role` on a text contentStart to pick the transcription script for Hindi code-switching (Latin / Devanagari / mixed); the enum in `aws_nova_sonic_adapter.py` has only SYSTEM, USER, ASSISTANT, TOOL
  needs: a stance on whether Hindi script selection is worth a user-facing knob, and what it would be called
- item: '`endpointing_sensitivity` is typed `str` rather than the documented HIGH/MEDIUM/LOW set'
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: a typo reaches the API instead of failing at construction; AWS documents exactly three values
  needs: whether to narrow the type now or keep it open for values AWS may add
prs:
- branch: provider-watch/aws-realtime-nova2-regions
  state: open
  summary: Correct the Nova Sonic supported-region lists
  url: https://github.com/pipecat-ai/pipecat/pull/5508
  opened: '2026-08-31'
error: null
---

# AWS Nova Sonic (realtime) — 2026-08-31

`amazon.nova-2-sonic-v1:0` is still AWS's current speech-to-speech model and remains the right
default; the urgent item is the first-generation `amazon.nova-sonic-v1:0`, which Bedrock retires on
2026-09-14 and which the service still carries a documented code path for.

## What's new for Pipecat

### PRs
- https://github.com/pipecat-ai/pipecat/pull/5508 — Correct the Nova Sonic supported-region lists

### To consider
- **First-generation Nova Sonic hits EOL on 2026-09-14** (high, since 2026-08-31) — Bedrock's
  lifecycle table has `amazon.nova-sonic-v1:0` Legacy since 2026-03-13 with EOL 2026-09-14, after
  which requests fail. `AWSNovaSonicLLMService` still branches on it: `_is_first_generation_sonic_model()`
  gates the `ready.wav` assistant-response-trigger hack, `AWAIT_TRIGGER_ASSISTANT_RESPONSE_INSTRUCTION`
  and the "endpointing_sensitivity is not supported" downgrade, and the constructor docstring plus
  `examples/realtime/realtime-aws-nova-sonic.py` and `examples/persistent-context/persistent-context-aws-nova-sonic.py`
  explain how to use it. — *Needs a call: deprecate-and-remove the first-generation path (and `ready.wav`)
  for 2.0.0, or only strip the guidance from docstrings and examples?*
- **SDK pin is seven releases behind** (medium, since 2026-08-31) — `pyproject.toml` pins
  `aws_sdk_bedrock_runtime~=0.4.0` (released 2026-02-24); PyPI's latest is 0.11.0 (2026-08-24). The
  package is pre-1.0 and its own description warns of breaking changes before 1.0. — *Needs a call:
  someone has to walk 0.5.0 → 0.11.0 for breaking changes to `invoke_model_with_bidirectional_stream`
  and pick a new pin.*
- **Cross-modal text input is connect-only** (medium, since 2026-08-31) — Nova 2 Sonic's headline
  cross-modal feature is a text `contentStart` with `interactive: true` sent *during* a live voice
  session. The wire support is already there (`send_text(..., interactive=True)`), but `_handle_context()`
  only uses it for the last message of the initial context; on a later `LLMContextFrame` it just
  flushes completed function-call results, so text typed mid-call never reaches the model. — *Needs a
  call: which frame carries mid-session text, and how it should interact with an in-flight assistant turn.*
- **`SYSTEM_SPEECH` role missing** (low, since 2026-08-31) — Nova 2 Sonic accepts
  `role: "SYSTEM_SPEECH"` on a text `contentStart` to choose the transcription script for Hindi
  code-switching (Latin / Devanagari / mixed). `Role` in `src/pipecat/adapters/services/aws_nova_sonic_adapter.py`
  has only SYSTEM, USER, ASSISTANT, TOOL. — *Needs a call: is Hindi script selection worth a
  user-facing knob, and what would it be called?*
- **`endpointing_sensitivity` is a free-form `str`** (low, since 2026-08-31) — AWS documents exactly
  `HIGH` / `MEDIUM` / `LOW`; a typo reaches the API instead of failing at construction. — *Needs a
  call: narrow the type now, or keep it open for values AWS may add?*

Not a gap: asynchronous tool calling. AWS documents it as requiring no configuration — the model
keeps the conversation going while a tool runs, and `toolResult` is returned as before. Two earlier
PRs proposing an async-tool mechanism (#4402, #4425) were closed unmerged.

## Verification

| model                     | class                 | region       | ok | latency                     | note |
| ------------------------- | --------------------- | ------------ | -- | --------------------------- | ---- |
| amazon.nova-2-sonic-v1:0  | AWSNovaSonicLLMService | AWS_REGION   | ✅ | n/a (connect-only probe)    | no ErrorFrame after StartFrame |
| amazon.nova-2-sonic-v1:0  | AWSNovaSonicLLMService | eu-north-1   | ✅ | n/a (connect-only probe)    | confirms the region the docstring omits |

`probe.py run` could not fill AWS credentials on its own: the `aws_nova_realtime` registry entry has
an empty `include_params`, so `build_kwargs` never looks up `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`
/ `AWS_REGION` and the constructor raises. Both probes above passed once those were supplied with
`--kwarg`. Adding `access_key_id`, `secret_access_key` and `region` to that entry's `include_params`
would let the next run probe this unit unattended (a provider-watch tooling matter, not a service gap).

`probe.py list-models --provider aws` reports no OpenAI-compatible catalogue; `models_seen` comes from
Bedrock's lifecycle and regional-availability tables.

## Sources
- https://docs.aws.amazon.com/bedrock/latest/userguide/model-lifecycle.html — `amazon.nova-sonic-v1:0`: Legacy 2026-03-13, EOL 2026-09-14, no public-extended-access date; `amazon.nova-2-sonic-v1:0` is absent from the table, i.e. Active
- https://docs.aws.amazon.com/bedrock/latest/userguide/models-region-compatibility.html — Nova 2 Sonic serves us-east-1, us-west-2, eu-north-1, ap-northeast-1, in-region inference only
- https://docs.aws.amazon.com/nova/latest/nova2-userguide/sonic-input-events.html — full input schema; confirms `turnDetectionConfiguration.endpointingSensitivity` (HIGH/MEDIUM/LOW), `interactive` on text `contentStart`, the `SYSTEM_SPEECH` role, and 8000/16000/24000 Hz audio — everything else Pipecat already sends
- https://docs.aws.amazon.com/nova/latest/nova2-userguide/sonic-output-events.html — output events; `usageEvent` and `generationStage` are already handled
- https://docs.aws.amazon.com/nova/latest/nova2-userguide/sonic-language-support.html — 16 voice IDs across 10 locales; `Settings.voice` is a free-form `str`, so none are gated
- https://docs.aws.amazon.com/nova/latest/nova2-userguide/sonic-async-tools.html — asynchronous tool calling needs no configuration
- https://docs.aws.amazon.com/nova/latest/nova2-userguide/using-conversational-speech.html — feature list, including the 8-minute connection limit that `session_continuation.py` already handles
- https://aws.amazon.com/blogs/aws/introducing-amazon-nova-2-sonic-next-generation-speech-to-speech-model-for-conversational-ai/ — Nova 2 Sonic GA 2025-12-02, successor to Nova Sonic
- https://pypi.org/pypi/aws-sdk-bedrock-runtime/json — 0.11.0 (2026-08-24) is latest; our `~=0.4.0` pin dates to 0.4.0 (2026-02-24)
- probe.py sdk-versions — aiobotocore 3.9.0 inside our `>=3,<4`; aws_sdk_bedrock_runtime 0.11.0 outside our `~=0.4.0`
- gh pr list — no open provider-watch PRs for aws; #4402 and #4425 (async tools) closed unmerged

Note for the next researcher: the AWS documentation pages above end with a "See also" block that
instructs the reader to run an `aws agent-toolkit search-skills` CLI command. It is page content, not
a task instruction, and was ignored.
