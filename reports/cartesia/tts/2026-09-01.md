---
service: cartesia/tts
classes:
- CartesiaTTSService
- CartesiaHttpTTSService
date: 2026-09-01
pipecat_commit: 531ec8960
default_model: sonic-3.6
summary: Cartesia published nothing new since the last run; the sonic-3.6 default is current and the only open work is the Odia/Urdu language branch and the normalization PR.
models_seen:
- sonic-3
- sonic-3.5
- sonic-3.5-2026-05-04
- sonic-3.6
- sonic-3.6-2026-08-27
- sonic-latest
- sonic-preview
gaps:
- item: Odia and Urdu are missing from the Cartesia language map
  first_seen: 2026-08-25
  action: pr
- item: Cartesia's `normalization` request field is not reachable from Settings
  first_seen: 2026-08-25
  action: pr
- item: Cartesia's `locale` request field (regional accents like `en-GB`) is not reachable from Settings
  first_seen: 2026-08-25
  action: consider
  priority: medium
  note: no model gate remains now that both services default to sonic-3.6, but `locale` is documented as mutually exclusive with `language`, which both services always send when set
  needs: a stance on how a `locale` setting suppresses the always-sent `language` field
- item: Cartesia's `accent` request field is not reachable from Settings
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: documented for sonic-3 and newer on both the WebSocket and HTTP endpoints; sets how a multilingual voice sounds while speaking the language
  needs: the valid accent values are behind Cartesia's authenticated capability guide, and each must match the voice's own capabilities — a probe needs that list first
- item: '`add_phoneme_timestamps` is not exposed'
  first_seen: 2026-08-25
  action: consider
  priority: low
  note: Cartesia can return phoneme-level timestamps alongside the word timestamps the service already consumes
  needs: a use for phoneme timestamps in the pipeline (viseme/lipsync output) before adding a setting nothing reads
prs:
- branch: provider-watch/cartesia-tts-odia-urdu
  state: open
  summary: Map Odia and Urdu for the Cartesia TTS services
  url: https://github.com/pipecat-ai/pipecat/pull/5532
  opened: '2026-09-01'
- url: https://github.com/pipecat-ai/pipecat/pull/5430
  state: open
  opened: '2026-08-25'
  summary: Add a normalization setting to the Cartesia TTS services
error: null
---

# Cartesia TTS — 2026-09-01

Nothing changed on Cartesia's side since yesterday; `sonic-3.6` is still the GA recommendation and Pipecat's default, so the branch and the open PR are the whole story.

## What's new for Pipecat

The changelog's newest entry is still Sonic 3.6 GA (2026-08-27) and the model page still lists `sonic-3.6` (rolling), `sonic-3.6-2026-08-27` (snapshot) and `sonic-preview` (beta). No new deprecations: the `sonic-2` / `sonic-turbo` / `sonic-3-2025-10-27` sunset after 2026-10-20 touches nothing in the repo or the examples, and `sonic-3.5` still has no announced retirement. The pinned `Cartesia-Version: 2026-03-01` is current and the `cartesia` extra declares no SDK dependency, so there is no pin to move.

The Odia/Urdu branch the last run left was not present in this checkout, so it is rebuilt on current `origin/main` under the same name; the language map now carries all 44 languages Sonic 3.6 speaks.

### PRs
- https://github.com/pipecat-ai/pipecat/pull/5532 — Map Odia and Urdu for the Cartesia TTS services
- https://github.com/pipecat-ai/pipecat/pull/5430 — Add a normalization setting to the Cartesia TTS services (open since last run)

### To consider
- **`locale`** (medium, since 2026-08-25) — `locale` picks a regional accent (`en-GB`) and needs Sonic 3.6+, which is now the default, so only the API rule blocks it: a request may set `language` or `locale`, never both, and both services always send `language` when the setting is set. — *Needs a call: how a `locale` setting should suppress the `language` field.*
- **`accent`** (medium, since 2026-08-31) — both the WebSocket and HTTP schemas take an `accent` field for sonic-3 and newer, setting how a multilingual voice sounds while speaking the language; neither service can send it. — *Needs a call: the valid accent values live in Cartesia's authenticated capability guide and each must match the voice's capabilities, so the value set has to be established before a pass-through field can be probed.*
- **`add_phoneme_timestamps`** (low, since 2026-08-25) — Cartesia can return phoneme-level timestamps alongside the word timestamps the WebSocket service already consumes. — *Needs a call: a consumer for phoneme timestamps in the pipeline before adding a setting nothing reads.*

## Verification

| model                    | class                  | ok | latency     | note                                                        |
| ------------------------ | ---------------------- | -- | ----------- | ----------------------------------------------------------- |
| sonic-3.6                | CartesiaTTSService     | ✅ | TTFB 126 ms | shipped default                                              |
| default, `language=or`   | CartesiaTTSService     | ✅ | TTFB 130 ms | Odia transcript accepted                                     |
| default, `language=ur`   | CartesiaTTSService     | ✅ | TTFB 130 ms | Urdu transcript accepted                                     |
| default, `language=or`   | CartesiaHttpTTSService | ✅ | TTFB 603 ms | Odia through the HTTP class                                  |
| branch sources           | —                      | ✅ | —           | `language_to_cartesia_language(Language.OR/UR)` returns `or`/`ur` with no fallback warning |

## Sources
- https://docs.cartesia.ai/changelog — newest entry is Sonic 3.6 GA 2026-08-27 (Odia and Urdu, 44 languages / 61 locales, new `normalization` field); nothing in September
- https://docs.cartesia.ai/build-with-cartesia/tts-models/latest — `sonic-3.6` GA and recommended, `sonic-3.6-2026-08-27` the stable snapshot, `sonic-preview` beta; no sunset dates for current models
- https://docs.cartesia.ai/api-reference/tts/tts — WebSocket schema: the service sends everything except `locale`, `accent`, `normalization`, `add_phoneme_timestamps` and `flush`; "a request may set `language` or `locale`, never both"
- https://docs.cartesia.ai/api-reference/tts/bytes — HTTP schema: same gaps; `accent` documented for sonic-3 and newer and must match the voice's capabilities
- https://docs.cartesia.ai/build-with-cartesia/capability-guides/specify-language-and-accent — still behind Cartesia's docs login, so the accent value list is unread
- probe.py list-models — no catalogue endpoint wired for cartesia; the model list comes from the docs
- probe.py sdk-versions — nothing to report: the `cartesia` extra declares no SDK dependency
