---
service: fal/stt
classes: [FalSTTService]
date: 2026-09-14
pipecat_commit: f6d601303
default_model: fal-ai/wizper
summary: Nothing new on fal's side; the two code bugs from 2026-09-01 still reproduce, plus a newly noticed one — the language map is never applied to the request.
models_seen:
  - fal-ai/cohere-transcribe
  - fal-ai/elevenlabs/forced-alignment
  - fal-ai/elevenlabs/speech-to-text
  - fal-ai/elevenlabs/speech-to-text/scribe-v2
  - fal-ai/smart-turn
  - fal-ai/speech-to-text
  - fal-ai/speech-to-text/stream
  - fal-ai/speech-to-text/turbo
  - fal-ai/speech-to-text/turbo/stream
  - fal-ai/wizper
  - nvidia/nemotron-asr-multilingual/asr
gaps:
  - item: Language auto-detect (`settings=Settings(language=None)`) produces no transcript
    first_seen: 2026-09-01
    action: consider
    priority: high
    note: "Wizper documents `language: null` as auto-detect; the request succeeds but `Language(self._settings.language)` in `run_stt` raises `None is not a valid Language`, so the utterance is swallowed into an ErrorFrame. Re-probed 2026-09-14, still fails."
    needs: "a call on what `TranscriptionFrame.language` should carry when fal auto-detects — the response has a `languages` list, not a single code"
  - item: The aiohttp session the service creates is never closed
    first_seen: 2026-09-01
    action: consider
    priority: medium
    note: "`_owns_session` is set in `__init__` but never read; there is no `stop`/`cancel`/`cleanup` closing `self._session`, so every probe run ends with `Unclosed client session`. `cartesia/tts.py` has the pattern to copy. Only affects callers that do not pass `aiohttp_session=` (the CLI/registry path); `examples/voice/voice-fal.py` passes its own."
    needs: "which lifecycle hooks own the close (Cartesia closes in both `stop` and `cancel`) — a behavior change, not a value change"
  - item: "`run_stt` sends `settings.language` raw, so `language_to_fal_language` is never applied"
    first_seen: 2026-09-14
    action: consider
    priority: low
    note: "the payload is built with `payload[\"language\"] = self._settings.language` rather than `self.language_to_service_language(...)`, so the LANGUAGE_MAP and its base-code fallback/warning are dead code. Probed `pt-BR` and `en-US`: fal accepts both (pt-BR transcribed as Portuguese), so nothing breaks today, but any string fal does not accept reaches the API unmapped instead of falling back to its base code."
    needs: "whether to route through `language_to_service_language` — it changes the wire value for regional codes fal currently tolerates"
  - item: Only `fal-ai/wizper` is reachable; `Settings.model` is ignored
    first_seen: 2026-09-01
    action: consider
    priority: low
    note: "the endpoint URL is hard-coded and `Settings(model=...)` does nothing; fal also serves Canary (`fal-ai/speech-to-text{,/turbo,/stream,/turbo/stream}`), ElevenLabs Scribe v2 and Cohere Transcribe, and the streaming variants would suit a non-segmented service"
    needs: "whether Fal support should stay Wizper-only (the registry label is `Fal (Wizper)`) or grow model routing / a streaming class — schemas differ per endpoint"
  - item: Wizper's `max_segment_len` and `merge_chunks` are not exposed
    first_seen: 2026-09-01
    action: consider
    priority: low
    note: "the two request fields we never send; both only shape chunking of long audio (`max_segment_len` is capped at 29 s), and `SegmentedSTTService` posts single utterances and reads only `text`"
    needs: "whether they are worth Settings fields at all given segmented input, or should stay unexposed"
prs: []
error: null
---

# Fal STT — 2026-09-14

No provider-side change since 2026-09-01: Wizper is current and its schema is byte-identical. Everything open is in our own code, and one more item joins the list.

## What's new for Pipecat

### PRs
Nothing.

### To consider
- **Language auto-detect errors out** (high, since 2026-09-01) — Wizper documents `language: null` as auto-detect and the request does succeed, but `Language(self._settings.language)` in `run_stt` raises `None is not a valid Language`; the handler turns the whole utterance into an ErrorFrame, so the user gets no transcript. Re-probed today, unchanged. — *Needs a call: what `TranscriptionFrame.language` should carry when fal auto-detects — the response returns a `languages` list rather than a single code.*
- **Leaked aiohttp session** (medium, since 2026-09-01) — the service creates its own `ClientSession` when none is passed and records `_owns_session`, but nothing reads that flag: no `stop`/`cancel`/`cleanup` closes it, and every probe run today still ended with `Unclosed client session`. `cartesia/tts.py` closes an owned session in both `stop` and `cancel`. — *Needs a call: which lifecycle hooks own the close; this is a behavior change rather than a value change.*
- **Language map never applied** (low, since 2026-09-14) — `run_stt` puts `self._settings.language` into the payload directly, bypassing `language_to_service_language`, so the 96-entry `LANGUAGE_MAP` and `resolve_language`'s base-code fallback are unreachable. Probes with `en-US` and `pt-BR` both returned 200 with the right transcript (fal normalizes regional codes itself), so this is latent rather than user-visible — but an unmapped code fal does not recognise reaches the API instead of degrading to its base code. — *Needs a call: whether to route the payload through `language_to_service_language`, which changes the wire value for regional codes fal currently tolerates.*
- **Wizper-only endpoint** (low, since 2026-09-01) — `https://fal.run/fal-ai/wizper` is hard-coded and `Settings(model=...)` is accepted but ignored, so fal's other transcription endpoints (Canary `fal-ai/speech-to-text` with its `/turbo`, `/stream` and `/turbo/stream` variants, ElevenLabs Scribe v2, Cohere Transcribe) are unreachable. — *Needs a call: keep Fal support Wizper-only, as the registry label `Fal (Wizper)` implies, or add model routing / a streaming class; the endpoints have different request schemas.*
- **`max_segment_len` / `merge_chunks` unexposed** (low, since 2026-09-01) — the only two Wizper request fields the service never sends; both only shape chunking of long audio (`max_segment_len` is bounded 10–29 s), while `SegmentedSTTService` posts one utterance at a time and reads only `text`. — *Needs a call: whether they earn Settings fields given segmented input.*

Checked and clear: the live Wizper schema is unchanged from the last run — `version` is still `const: "3"`, `chunk_level` still `const: "segment"`, the language enum still holds the same 99 codes. Wizper reports `deprecated: false` in fal's model index and is still labelled Experimental with no successor. The only catalogue addition since 2026-09-01 is `fal-ai/elevenlabs/forced-alignment` (2026-09-08), which is alignment, not transcription. The `fal` extra pulls no SDK (`fal = []` in `pyproject.toml`), so there is no pin to compare, and `src/pipecat/services/fal/stt.py` has not changed since the last report.

## Verification

| model         | class         | ok | latency      | note                                                                          |
| ------------- | ------------- | -- | ------------ | ----------------------------------------------------------------------------- |
| fal-ai/wizper | FalSTTService | ✅ | TTFB 567 ms  | default settings; transcript correct                                            |
| fal-ai/wizper | FalSTTService | ✅ | TTFB 9015 ms | `--setting language=en` — correct transcript                                    |
| fal-ai/wizper | FalSTTService | ✅ | TTFB 11761 ms | `--setting language=en-US` — accepted unmapped, correct transcript             |
| fal-ai/wizper | FalSTTService | ✅ | TTFB 5866 ms | `--setting language=pt-BR` — accepted unmapped, transcribed as Portuguese       |
| fal-ai/wizper | FalSTTService | ❌ | —            | `--setting language=null`: `Unknown error occurred: None is not a valid Language` |

TTFB on fal's side swung between 0.6 s and 11.8 s across runs a minute apart, so these numbers say nothing about the language values; they are pass/fail evidence only. Every run printed `Unclosed client session` on shutdown.

## Sources
- https://fal.ai/api/openapi/queue/openapi.json?endpoint_id=fal-ai/wizper — live Wizper request schema, identical to 2026-09-01: `audio_url`, `task`, `language` (99-code enum plus `null` for auto-detect), `chunk_level` (`const segment`), `max_segment_len` (10–29, default 29), `merge_chunks` (default true), `version` (`const "3"`); response carries `text`, `chunks`, `languages`
- https://fal.ai/api/models?categories=speech-to-text — fal's STT catalogue with per-entry `date` and `deprecated`; all eleven entries report `deprecated: false`, Wizper dated 2024-04-08, newest entry `fal-ai/elevenlabs/forced-alignment` 2026-09-08
- https://fal.ai/models/fal-ai/wizper/api — Wizper still Experimental, no deprecation notice, same seven documented inputs
- WebSearch "fal.ai changelog speech-to-text" — no STT release since the last run; blog.fal.ai's recent posts are TTS/video
- probe.py list-models --provider fal — exit 0, "no model catalogue known for 'fal'"
- probe.py sdk-versions --provider fal — nothing to report; the `fal` extra declares no dependencies
</content>
