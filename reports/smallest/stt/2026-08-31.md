---
service: smallest/stt
classes:
- SmallestSTTService
date: 2026-08-31
pipecat_commit: 6c94c365d
default_model: pulse
summary: Pulse is still the only streaming model; a branch adds two documented realtime parameters, and the language map advertises codes the streaming endpoint rejects.
models_seen:
- pulse
- pulse-pro
gaps:
- item: Pulse's `eou_timeout_ms` is not reachable from Settings
  first_seen: 2026-08-31
  action: pr
- item: Pulse's `itn_normalize` is not reachable from Settings
  first_seen: 2026-08-31
  action: pr
- item: LANGUAGE_MAP does not match Pulse's streaming language set
  first_seen: 2026-08-31
  action: consider
  priority: high
  note: 15 mapped codes (bg, cs, da, et, fi, hu, lt, lv, mt, pa, pl, ro, sk, sv, uk) are pre-recorded-only or undocumented; `language=pl` fails the WebSocket handshake and surfaces as an opaque reconnection error. The four streaming East Asian codes (zh, yue, ja, ko) and the aggregators (north_indic, multi-asian, multi-south-indic) are absent, so they only work via resolve_language's unverified fallback.
  needs: trimming the map is a breaking change for anyone passing those codes today; decide between removing them, raising a clear error, or documenting the streaming/batch split
- item: '`finalize_on_words` and `max_words` are not reachable from Settings'
  first_seen: 2026-08-31
  action: consider
  priority: medium
  note: Pulse's documented agentic pattern is `finalize_on_words=false` plus a client `finalize` per turn, which is exactly what the service already sends on VADUserStoppedSpeakingFrame.
  needs: these change when finals arrive, so exposing them needs a stance on whether the service should adopt the agentic pattern by default rather than just pass the flags through
- item: '`vad_events` speech_started / speech_ended messages are not surfaced'
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: '`vad_events=true` interleaves acoustic `speech_started` / `speech_ended` JSON messages with transcriptions; `_process_response` reads only `transcript`, so they would be dropped.'
  needs: decide whether server VAD events should map to Pipecat frames at all, given the pipeline already runs its own VAD
- item: '`numerals=auto` is sent on every connection but is not in Pulse''s parameter reference'
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: The AsyncAPI spec for GET /waves/v1/stt/live lists no `numerals` parameter; `itn_normalize` is the documented way to get written-form numbers.
  needs: confirm with Smallest whether `numerals` is still honored before dropping the field or redirecting users to `itn_normalize`
- item: '`punctuate` and `capitalize` are not reachable from Settings'
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: Both default to true and split what the single `format` field controls today.
  needs: decide whether the finer-grained pair is worth two fields next to `format`, which the docs still present as the one formatting switch
- item: Pulse Pro has no Pipecat path
  first_seen: 2026-08-31
  action: consider
  priority: low
  note: 'pulse-pro is GA (English-only, 5.42% ESB WER, #2 on the Open ASR Leaderboard) but batch-only; the streaming endpoint rejects it with 400. Reaching it needs an HTTP class against POST /waves/v1/stt/.'
  needs: a new service class is out of scope for provider-watch; decide whether a batch Smallest STT service is wanted
prs:
- branch: provider-watch/smallest-stt-realtime-params
  state: open
  summary: Add eou_timeout_ms and itn_normalize to SmallestSTTService settings
  url: https://github.com/pipecat-ai/pipecat/pull/5518
  opened: '2026-08-31'
error: null
---

# Smallest STT — 2026-08-31

`pulse` stays the default — it is the only model the streaming endpoint accepts. Two documented realtime
parameters are on a branch; the language map is the one thing that can break a user today.

## What's new for Pipecat

### PRs
- https://github.com/pipecat-ai/pipecat/pull/5518 — Add eou_timeout_ms and itn_normalize to SmallestSTTService settings

### To consider
- **Language map vs. Pulse's streaming set** (high, since 2026-08-31) — 15 mapped codes are pre-recorded-only or undocumented on streaming; a probe with `language=pl` fails the WebSocket handshake and the user sees only a reconnection error. The streaming East Asian codes (`zh`, `yue`, `ja`, `ko`) and the three aggregators are missing, so they reach the API only through `resolve_language`'s unverified fallback. — *Needs a call: trimming the map breaks anyone passing those codes today, so choose between removing them, failing with a clear message, or documenting the streaming/batch split.*
- **Finalize control** (medium, since 2026-08-31) — `finalize_on_words` and `max_words` are absent from `Settings`; Pulse documents `finalize_on_words=false` plus a per-turn client `finalize` as the voice-agent pattern, and the service already sends that `finalize` on `VADUserStoppedSpeakingFrame`. — *Needs a call: they change when finals arrive, so it is a question of whether the service should adopt the agentic pattern by default rather than only pass the flags through.*
- **VAD events** (low, since 2026-08-31) — `vad_events=true` interleaves `speech_started` / `speech_ended` messages on the same socket; `_process_response` reads only `transcript`, so they would be dropped. — *Needs a call: whether server VAD events should become frames at all, given the pipeline runs its own VAD.*
- **`numerals` is undocumented** (low, since 2026-08-31) — the service always sends `numerals=auto`, but the AsyncAPI spec for the live endpoint has no such parameter and points at `itn_normalize` for written-form numbers. — *Needs a call: confirm with Smallest whether `numerals` is still honored before dropping the field.*
- **`punctuate` / `capitalize`** (low, since 2026-08-31) — the spec documents both alongside `format`, which is all `Settings` exposes. — *Needs a call: whether the finer-grained pair earns two fields next to `format`.*
- **Pulse Pro** (low, since 2026-08-31) — GA, English-only, batch-only; the streaming endpoint rejects `model=pulse-pro` with 400, so using it needs an HTTP class against `POST /waves/v1/stt/`. — *Needs a call: a new service class is outside provider-watch's remit; decide whether a batch Smallest STT service is wanted.*

## Verification

| model | class              | ok | latency     | note                                                             |
| ----- | ------------------ | -- | ----------- | ---------------------------------------------------------------- |
| pulse | SmallestSTTService | ✅ | TTFB 647 ms | branch defaults, no new setting sent                             |
| pulse | SmallestSTTService | ✅ | TTFB 669 ms | `--setting eou_timeout_ms=300`                                   |
| pulse | SmallestSTTService | ✅ | TTFB 658 ms | `--setting itn_normalize=true`                                   |
| pulse | SmallestSTTService | ❌ | —           | `--setting language=pl` — WebSocket handshake rejected           |
| pulse | SmallestSTTService | ✅ | TTFB 765 ms | `--setting language=hi` — control for the rejection above        |
| pulse | SmallestSTTService | ✅ | TTFB 674 ms | `--setting language=ja` — accepted on `api.smallest.ai` although the docs call it US-region only |

## Sources
- https://docs.smallest.ai/models/api-reference/speech-to-text/speech-to-text — AsyncAPI spec for `GET /waves/v1/stt/live`: the full query-parameter set (`vad_events`, `eou_timeout_ms`, `itn_normalize`, `finalize_on_words`, `max_words`, `punctuate`, `capitalize`, …), the `finalize` / `close_stream` control messages, and no `numerals`
- https://docs.smallest.ai/models/model-cards/speech-to-text/pulse — 21 streaming languages + 3 aggregators vs. 22 pre-recorded, region restrictions; Pulse is current and not deprecated
- https://docs.smallest.ai/models/model-cards/speech-to-text/pulse-pro — pulse-pro is GA but pre-recorded/HTTP only, not a Pulse successor
- https://docs.smallest.ai/models/documentation/speech-to-text-pulse/overview — only `pulse` is accepted on the live WebSocket; `pulse-pro` returns 400
- https://docs.smallest.ai/models/documentation/speech-to-text-pulse/features/end-of-utterance-timeout — `eou_timeout_ms` range 100–10000, default 800
- https://docs.smallest.ai/models/documentation/speech-to-text-pulse/features/inverse-text-normalization — `itn_normalize` default false, pairs with `finalize_on_words`
- https://docs.smallest.ai/models/documentation/speech-to-text-pulse/features/finalize-control — `finalize_on_words` / `max_words` semantics and the agentic finalize pattern
- https://docs.smallest.ai/models/documentation/speech-to-text-pulse/features/vad-events — `speech_started` / `speech_ended` payloads, WebSocket only
- https://docs.smallest.ai/models/documentation/speech-to-text-pulse/features/endpointing — `endpointing` on by default, how it interacts with `eou_timeout_ms`
- probe.py list-models — no catalogue for smallest (exit 3); model list taken from the docs
- probe.py sdk-versions — the smallest extra pulls in no provider SDK, so nothing to compare
