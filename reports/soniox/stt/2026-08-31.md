---
service: soniox/stt
classes:
- SonioxSTTService
date: 2026-08-31
pipecat_commit: 6c94c365d
default_model: stt-rt-v5
summary: Nothing changed at Soniox this week; stt-rt-v5 stays current, a branch corrects the stale context_version docstrings, and real-time translation remains the one unreachable API affordance.
models_seen:
- stt-async-v5
- stt-rt-v5
gaps:
- item: Soniox's real-time `translation` config object is not reachable from Settings
  first_seen: 2026-08-25
  action: consider
  priority: medium
  note: 'v5 streams translation tokens on the same socket; the config takes `{type: one_way, target_language}` or `{type: two_way, language_a, language_b}`'
  needs: translated tokens arrive interleaved with originals and marked `translation_status`, so exposing this needs a decision on how translations reach the pipeline (separate frames, or filtered out of the transcript)
- item: '`SonioxContextObject` docstring anchors context_version 2 to the retired `stt-rt-v3-preview`'
  first_seen: 2026-08-25
  action: pr
prs:
- branch: provider-watch/soniox-stt-context-docs
  state: branch
  summary: Describe Soniox context by its two accepted forms
  capped: true
error: null
---

# Soniox STT — 2026-08-31

Current: `stt-rt-v5` is still Soniox's recommended real-time model and nothing is retiring; the only change proposed is a docstring correction, now that a probe settles which `context` forms v5 accepts.

## What's new for Pipecat

### PRs
- `provider-watch/soniox-stt-context-docs` — review: `git show provider-watch/soniox-stt-context-docs` — Describe Soniox context by its two accepted forms

### To consider
- **Real-time translation** (medium, since 2026-08-25) — v5 does transcription and translation on one stream via a `translation` config object (`one_way` with `target_language`, or `two_way` with `language_a`/`language_b`); `SonioxSTTSettings` has no field for it and `_connect_websocket` never sends the key. — *Needs a call: translation tokens arrive interleaved with the originals and are distinguished only by `translation_status`, with no timestamps, so `_receive_messages` would have to decide whether they become their own frames or are dropped from the transcript — not a pass-through field.*

Last run's low-priority context-wording item is the branch above: a probe confirms `stt-rt-v5` accepts a plain-string `context`, so the string branch in `_connect_websocket` stays and only the docstrings change.

## Verification

| model     | class            | ok | latency      | note                                                                              |
| --------- | ---------------- | -- | ------------ | --------------------------------------------------------------------------------- |
| stt-rt-v5 | SonioxSTTService | ✅ | TTFB 4112 ms | baseline; full utterance transcribed correctly, TTFB spans the scripted phrase      |
| stt-rt-v5 | SonioxSTTService | ✅ | TTFB 4122 ms | `--setting context=<plain string>`: accepted, transcript returned, no error frame  |
| stt-rt-v5 | SonioxSTTService | ❌ | —            | control: `--kwarg audio_format=not_a_real_format` → Soniox 400, so it does validate the config message and the string `context` above was not silently tolerated |

`probe.py list-models --provider soniox` reports no catalogue (Soniox publishes no models endpoint); the model list comes from the docs. The `soniox` extra pins no SDK — the service speaks the WebSocket API directly — so `sdk-versions` has nothing to compare.

## Sources
- https://soniox.com/docs/stt/models — unchanged: `stt-rt-v5` and `stt-async-v5` are the only active models, `stt-rt-v4`/`stt-async-v4` retired 2026-06-30 and alias to v5, v3 retired 2025-11-30
- https://soniox.com/docs/stt/api-reference/websocket-api — full config parameter list; `translation` is still the only key `_connect_websocket` does not send
- https://soniox.com/docs/stt/rt/real-time-translation — translation config shape and `translation_status`; translated tokens carry no `start_ms`/`end_ms`
- https://soniox.com/docs/stt/concepts/context — context documented as an object with `general`/`text`/`terms`/`translation_terms`; no context versions anywhere in the page
- https://github.com/soniox/soniox-python/blob/main/CHANGELOG.md — 2.9.0 (2026-08-11) and 2.8.0 (2026-06-30) add TTS and account-usage features only; the last STT config addition was `endpoint_latency_adjustment_level` in 2.7.0, already a Setting
- https://soniox.com/docs/data-residency — regional hosts (`wss://stt-rt.eu.soniox.com/transcribe-websocket` and equivalents for jp/in) select data residency; reachable today through the service's `url` constructor argument, so no code gap
- probe.py run — baseline, string-`context`, and an invalid-`audio_format` control against `SonioxSTTService`
