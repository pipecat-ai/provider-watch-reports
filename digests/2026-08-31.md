# Provider watch — 2026-08-31

- Sarvam retired `bulbul:v2`, so both Sarvam TTS classes now fail against the live API out of the box — `provider-watch/sarvam-tts-bulbul-v3` moves the default to `bulbul:v3` and is the branch to review first.
- `SpeechmaticsSTTService` raises at construction when `split_sentences` is set; `provider-watch/speechmatics-stt-include-results` fixes the crash and adds an `include_results` field.
- `sarvam/llm` needs a maintainer call rather than a branch: the default `sarvam-105b` reasons on every turn (10219 ms TTFAT median vs 888 ms for `sarvam-105b-conversations`) and `Settings` cannot send the `reasoning_effort: null` that would turn it off.

**30 units researched** — 11 PRs, 7 branches, 113 changes to consider, 3 errors, 0 with nothing new.

## PRs to review

- [assemblyai/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/assemblyai/stt/2026-08-31.md) — https://github.com/pipecat-ai/pipecat/pull/5507 — Name AssemblyAI's current U3 Pro streaming model in examples and docs
- [asyncai/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/asyncai/tts/2026-08-31.md) — https://github.com/pipecat-ai/pipecat/pull/5464 — Apply model, voice and language changes by opening a new session (covers the settings-update no-op)
- [aws/realtime](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/realtime/2026-08-31.md) — https://github.com/pipecat-ai/pipecat/pull/5508 — Correct the Nova Sonic supported-region lists
- [aws/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/stt/2026-08-31.md) — https://github.com/pipecat-ai/pipecat/pull/5509 — Map the full AWS Transcribe streaming language set
- [aws/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/stt/2026-08-31.md) — https://github.com/pipecat-ai/pipecat/pull/2849 — Add language options in AWS Transcribe STT Service (community PR, untouched since 2025-10-16)
- [aws/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/tts/2026-08-31.md) — https://github.com/pipecat-ai/pipecat/pull/5510 — Map Language.EN_IE and Language.EN_SG for AWS Polly
- [azure/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/stt/2026-08-31.md) — https://github.com/pipecat-ai/pipecat/pull/5511 — Add segmentation_silence_timeout_ms to AzureSTTService settings
- [cartesia/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/tts/2026-08-31.md) — https://github.com/pipecat-ai/pipecat/pull/5430 — Add a normalization setting to the Cartesia TTS services
- [sarvam/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/tts/2026-08-31.md) — https://github.com/pipecat-ai/pipecat/pull/5517 — Default Sarvam TTS to bulbul:v3 and send the REST sample rate under the field Sarvam reads
- [smallest/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/stt/2026-08-31.md) — https://github.com/pipecat-ai/pipecat/pull/5518 — Add eou_timeout_ms and itn_normalize to SmallestSTTService settings
- [smallest/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/tts/2026-08-31.md) — https://github.com/pipecat-ai/pipecat/pull/5519 — Match SmallestTTSService's language map to the Waves API

## Branches not opened as PRs (per-run cap reached)

- [cartesia/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/stt/2026-08-31.md) — `provider-watch/cartesia-stt-finalization-controls` — review: `git show provider-watch/cartesia-stt-finalization-controls` — Expose Cartesia's silence-based finalization controls
- [cartesia/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/tts/2026-08-31.md) — `provider-watch/cartesia-tts-odia-urdu` — review: `git show provider-watch/cartesia-tts-odia-urdu` — Map Odia and Urdu for the Cartesia TTS services
- [cartesia/turns-stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/turns-stt/2026-08-31.md) — `provider-watch/cartesia-turns-stt-turn-thresholds` — review: `git show provider-watch/cartesia-turns-stt-turn-thresholds` — Expose Cartesia Ink 2 turn detection thresholds
- [soniox/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/soniox/stt/2026-08-31.md) — `provider-watch/soniox-stt-context-docs` — review: `git show provider-watch/soniox-stt-context-docs` — Describe Soniox context by its two accepted forms
- [soniox/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/soniox/tts/2026-08-31.md) — `provider-watch/soniox-tts-reduce-silence` — review: `git show provider-watch/soniox-tts-reduce-silence` — Add a reduce_silence setting to SonioxTTSService
- [speechmatics/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/stt/2026-08-31.md) — `provider-watch/speechmatics-stt-include-results` — review: `git show provider-watch/speechmatics-stt-include-results` — Fix the split_sentences crash and expose Speechmatics word-level results
- [speechmatics/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/tts/2026-08-31.md) — `provider-watch/speechmatics-tts-sm-app-version` — review: `git show provider-watch/speechmatics-tts-sm-app-version` — Report Pipecat's version in the Speechmatics TTS sm-app tag

## Changes to consider

**High**

- [anthropic/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/anthropic/llm/2026-08-31.md) — Anthropic's `output_config.effort` is not reachable from Settings — Anthropic tells Sonnet 4.6 users to set effort explicitly "to avoid unexpected latency"; our default model is Sonnet 4.6 and we never set it. Probed on a scheduling question: TTFAT 4002 ms at the provider default (`high`) against 1264 ms with `extra={"output_config": {"effort": "low"}}`. Today `extra` is the only way through. — *needs a call: whether to expose Anthropic's nested `output_config` or a flat `effort` field, and whether Pipecat should pick a low-latency level on Sonnet 4.6 the way `_maybe_disable_thinking` already does for Sonnet 5*
- [asyncai/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/asyncai/tts/2026-08-31.md) — A websocket TTS turn synthesizes but the pipeline never finishes (EndFrame never reaches the end) — probe.py runs against AsyncAITTSService produce audio (TTFB 241 ms, TTFA 441 ms) and then hang; the harness cancels after ~79 s and the process still does not exit. CartesiaTTSService through the same harness exits cleanly. The keepalive task keeps sending `{"transcript": " "}` throughout. — *needs a call: whether the stall is in this service's context close / keepalive teardown or in the shared websocket TTS base*
- [aws/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/llm/2026-08-31.md) — Claude Sonnet 5 and later run adaptive thinking by default on Bedrock; the service has no thinking-off default — measured through AWSBedrockLLMService, TTFAT 1826 ms (median of 3) for us.anthropic.claude-sonnet-5 vs 920 ms for us.anthropic.claude-sonnet-4-6; AnthropicLLMService already forces `thinking: disabled` on Sonnet 5+ (#5446), AWSBedrockLLMService does not — *needs a call: a stance on how to recognize reasoning-on-by-default models from a Bedrock model id, and whether the fix is a `thinking`-style Settings field or an implicit default like the Anthropic service's*
- [aws/other](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/other/2026-08-31.md) — `invoke_agent_runtime` is called without `runtimeSessionId`, so turns are not pinned to one AgentCore session — AWS: "Without a consistent session ID, each request may be routed to a new microVM, which may result in additional latency due to cold starts"; context is preserved only between invocations sharing a session ID. The default payload transformer sends only the latest user message, so with no session ID the agent has neither Pipecat-side history nor AgentCore-side continuity. — *needs a call: Where the ID comes from — a constructor argument, or one generated per conversation (AgentCore requires 33+ characters) — and whether teardown should call StopRuntimeSession.*
- [aws/realtime](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/realtime/2026-08-31.md) — First-generation amazon.nova-sonic-v1:0 reaches Bedrock EOL on 2026-09-14, but the service still carries and documents a support path for it — `_is_first_generation_sonic_model()` gates the `ready.wav` assistant-response-trigger hack, `AWAIT_TRIGGER_ASSISTANT_RESPONSE_INSTRUCTION` and the `endpointing_sensitivity` downgrade warning; the docstring and three examples tell users how to run the older model. Bedrock lists it Legacy since 2026-03-13, EOL 2026-09-14, after which requests fail. — *needs a call: a call on whether to deprecate-and-remove the first-generation code path (and `ready.wav`) for 2.0.0 or only strip the guidance from docstrings and examples*
- [aws/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/stt/2026-08-31.md) — Sample rates other than 8000/16000 Hz are relabelled 16000 Hz instead of resampled — `_connect_websocket` logs "Converting from 24000 Hz to 16000 Hz" and declares 16000 in the presigned URL, but `run_stt` sends the pipeline's bytes untouched, so AWS decodes them at the wrong rate. AWS accepts 8000-48000 Hz: an ad-hoc connect at 24000 Hz was accepted and transcribed. — *needs a call: Which fix — pass the pipeline rate through when it is within 8000-48000 Hz, or resample to 16 kHz the way `OpenAIRealtimeSTTService` does with `create_stream_resampler`.*
- [azure/image](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/image/2026-08-31.md) — Service calls the retired `images/generations:submit` endpoint with api-version 2023-06-01-preview — `run_image_gen` posts to `{endpoint}openai/images/generations:submit?api-version=2023-06-01-preview` and polls `operation-location`. That api-version retired 2024-07-01 and the submit/poll flow was DALL-E 2's; dall-e-2 retired 2025-01-27 and dall-e-3 on 2026-03-04 with existing deployments non-functional. Azure's current image models (gpt-image-2 GA, gpt-image-1/-mini/-1.5 preview) are served synchronously by `POST {endpoint}openai/v1/images/generations`. The request also never sends `model`, so `AzureImageGenSettings.model` is silently ignored, and the response is read as `result.data[0].url` while gpt-image-* returns `b64_json` only. — *needs a call: Porting to the v1 images endpoint changes the request, the response decoding (base64 to `URLImageRawFrame` or a plain image frame) and the polling loop at once - a behavioral rewrite that needs a maintainer's call on scope and a live Azure image deployment to verify; no AZURE_DALLE_API_KEY is configured here.*
- [azure/realtime](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/realtime/2026-08-31.md) — Example bot and CLI scaffold never set Settings.model, so the URL carries OpenAI's default gpt-realtime-2.1 instead of the Azure deployment — `examples/realtime/realtime-azure.py` and the `azure_realtime` entries in `src/pipecat/cli/registry/_configs.py` / `service_metadata.py` build the service with no `model`; the base class appends `?model=gpt-realtime-2.1`, which is not an Azure model and is unlikely to be a user's deployment name. Azure's newest realtime model is `gpt-realtime-2` (2026-05-07); 2.1 does not exist there. — *needs a call: which env var should name the deployment (add `AZURE_REALTIME_MODEL` to the scaffold's env template?), and whether the service should refuse to connect when no deployment is named*
- [cartesia/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/stt/2026-08-31.md) — Default ink-whisper is Cartesia's older STT model; ink-2 is the stable recommended one — ink-2 works on the manual `/stt/websocket` endpoint through this class, but it is English-only and measured TTFS 509 ms against 102 ms for ink-whisper (medians of 5); Cartesia's own answer for ink-2 is the auto-finalizing `/stt/turns/websocket` endpoint, which Pipecat already ships as `CartesiaTurnsSTTService`. No sunset date is announced for ink-whisper, and batch `/stt` still requires it. — *needs a call: whether the manual-endpoint service should follow ink-2 despite English-only support and the TTFS regression, or stay on ink-whisper and leave ink-2 to CartesiaTurnsSTTService*
- [sarvam/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/llm/2026-08-31.md) — Reasoning cannot be turned off on the default sarvam-105b — Sarvam defaults reasoning_effort to medium, so a trivial prompt costs TTFAT 10219 ms (median of 5, thinking 9301 ms). Sarvam documents disabling it by sending reasoning_effort null; on /v1 that works (805 ms vs 15.1 s), on /v2 it is ignored (681 reasoning tokens). SarvamLLMSettings.reasoning_effort=None means do-not-send, so the null can never reach the API, and the service routes sarvam-105b to /v2 anyway. — *needs a call: sending an explicit null needs a sentinel or a Sarvam-specific default, and honouring it needs sarvam-105b routed to /v1 — both change behavior for existing users*
- [smallest/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/stt/2026-08-31.md) — LANGUAGE_MAP does not match Pulse's streaming language set — 15 mapped codes (bg, cs, da, et, fi, hu, lt, lv, mt, pa, pl, ro, sk, sv, uk) are pre-recorded-only or undocumented; `language=pl` fails the WebSocket handshake and surfaces as an opaque reconnection error. The four streaming East Asian codes (zh, yue, ja, ko) and the aggregators (north_indic, multi-asian, multi-south-indic) are absent, so they only work via resolve_language's unverified fallback. — *needs a call: trimming the map is a breaking change for anyone passing those codes today; decide between removing them, raising a clear error, or documenting the streaming/batch split*

**Medium**

- [anthropic/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/anthropic/llm/2026-08-31.md) — The `anthropic>=0.49.0,<1` pin excludes the SDK's current major (1.2.0, 2026-08-27) — SDK 1.x drops `temperature`, `top_p` and `top_k` from the request types (passing them raises TypeError) and moves the client to httpx2; the service puts all three in every request, so the pin bump is not mechanical. 1.1.0 also removed the legacy response API and added an `updates` thinking display mode — *needs a call: a decision on dropping the sampling parameters from the Anthropic request path, which changes what Settings.temperature and friends do*
- [assemblyai/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/assemblyai/stt/2026-08-31.md) — Streaming PII redaction (redact_pii, redact_pii_policies, redact_pii_sub) is not reachable from Settings — shipped 2026-05-15 on every streaming model; three connect params plus `include_partial_turns`, which the server forces to false when redaction is on — *needs a call: redaction suppresses partial turns, so a stance is needed on what the service does with interim transcriptions when a user enables it*
- [assemblyai/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/assemblyai/stt/2026-08-31.md) — Only agent_context and language_codes are applied live; every other setting change tears down the websocket — AssemblyAI's UpdateConfiguration accepts prompt, keyterms_prompt, min_turn_silence, max_turn_silence, continuous_partials, vad_threshold, interruption_delay, mode, end_of_turn_confidence_threshold and session_heartbeat as well; HOT_UPDATABLE_SETTINGS lists two of the twelve — *needs a call: widening the set changes reconnect behavior mid-call, and the Pipecat turn mode rewrites min/max_turn_silence — needs a call on which fields are safe to hot-update*
- [assemblyai/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/assemblyai/stt/2026-08-31.md) — Unrecognized server messages become ErrorFrames — `_parse_message` raises on any type it does not know, and `_handle_message` turns that into `push_error`; the spec defines SpeakerRevision (sent at session end whenever speaker_labels is on), Heartbeat and LLMGatewayResponse — *needs a call: SpeakerRevision carries corrected speaker labels for turns already pushed downstream — ignoring it quietly and applying it are different features, so the fix needs a decision*
- [assemblyai/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/assemblyai/stt/2026-08-31.md) — Connect params max_speakers, inactivity_timeout, filter_profanity, include_partial_turns and session_heartbeat have no Settings fields — all five are documented optional scalars that would pass straight into `_build_ws_url` — *needs a call: session_heartbeat is only useful once Heartbeat is parsed, and include_partial_turns=false would starve the pipeline of interim frames — confirm which of the five the team wants exposed*
- [asyncai/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/asyncai/tts/2026-08-31.md) — Server error messages are dropped by the receive loop — Async documents error frames with `error_code` / `message` / `extra`, but `_receive_messages` only handles `final` and `audio`; everything else is logged as an unavailable context. Probed with the voice unset: the connection produced two non-audio frames, no ErrorFrame was pushed, and the turn hung until the harness gave up. — *needs a call: whether an `error_code` frame should push an ErrorFrame and close the context, or only be logged*
- [aws/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/llm/2026-08-31.md) — Bedrock `reasoningContent` deltas are dropped: no LLMThinking frames and no thinking metrics — _process_context only reads `delta["text"]` and `delta["toolUse"]`; a live stream from us.anthropic.claude-sonnet-5 carries `delta.reasoningContent` before its text, and a Nova 2 Lite turn with reasoning enabled can be reasoningContent-only — *needs a call: whether Bedrock reasoning should map onto the existing LLMThinking frames the Anthropic service emits, including whether reasoning blocks must be echoed back in later requests*
- [aws/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/llm/2026-08-31.md) — Default us.amazon.nova-lite-v1:0 is a generation behind Nova 2 Lite (GA 2025-12-02) — works unchanged through the class, TTFAT 601 ms vs 624 ms (median of 5) on a trivial prompt and 528 ms vs 490 ms on a reasoning prompt, reasoning off by default; nova-lite-v1:0 is still ACTIVE with no EOL date — *needs a call: aiewf-eval has not benchmarked any Amazon Nova model, so the leaderboard cannot confirm parity on aiwf_medium_context*
- [aws/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/llm/2026-08-31.md) — Bedrock Guardrails (`guardrailConfig`) are not reachable from Settings — ConverseStream takes guardrailIdentifier/guardrailVersion/trace/streamProcessingMode; the service never sends the block — *needs a call: no guardrail exists in the probe account to verify a pass-through against, and guardrail interventions arriving mid-stream need a stance on what the pipeline should see*
- [aws/other](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/other/2026-08-31.md) — InvokeAgentRuntime's qualifier, runtimeUserId, contentType/accept and trace headers are not reachable — The call passes only `agentRuntimeArn` and `payload`. `qualifier` selects a versioned endpoint instead of DEFAULT; `runtimeUserId` scopes a user for AgentCore Identity/Memory; `traceId`/`traceParent`/`traceState`/`baggage` carry distributed tracing into AgentCore observability. — *needs a call: The processor has no Settings class, so exposing these means either a new params object or several constructor arguments — a shape decision.*
- [aws/other](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/other/2026-08-31.md) — Transient AgentCore errors (409 RetryableConflictException, 429 ThrottlingException) are not retried — AWS documents 409 as brief and expected while a session is provisioned or torn down ("Retry with short exponential backoff"). The processor does not retry; the base FrameProcessor turns the exception into a generic ErrorFrame with category UNKNOWN and the turn is lost. — *needs a call: Whether retry belongs in the processor at all, given a voice turn's latency budget, and which errors should map to a rate-limit category rather than UNKNOWN.*
- [aws/realtime](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/realtime/2026-08-31.md) — aws_sdk_bedrock_runtime is pinned to ~=0.4.0 (2026-02-24) while 0.11.0 is current — seven minor releases behind; the package is pre-1.0 and its own PyPI description warns of breaking changes before 1.0, so the pin cannot simply be widened without reading each release — *needs a call: someone to walk 0.5.0 through 0.11.0 for breaking changes to invoke_model_with_bidirectional_stream and pick a new pin*
- [aws/realtime](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/realtime/2026-08-31.md) — Nova 2 Sonic cross-modal text input is only sent at connect, not during a live session — the wire support exists — `send_text(..., interactive=True)` — but `_handle_context()` only uses it for the last message of the *initial* context; a later `LLMContextFrame` only flushes completed function-call results, so text typed mid-call never reaches the model — *needs a call: a decision on which frame carries mid-session text (a new `LLMContextFrame` user message vs. an explicit append frame) and how it should interact with an in-flight assistant turn*
- [aws/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/stt/2026-08-31.md) — Most StartStreamTranscription request parameters are unreachable from Settings — `AWSTranscribeSTTSettings` adds no fields of its own. Unreachable: `vocabulary-name` and `vocabulary-filter-name` (already parameters of `get_presigned_url`, never passed by the service), `vocabulary-filter-method`, `content-redaction-type` / `content-identification-type` / `pii-entity-types`, `language-model-name`, `show-speaker-label` and `enable-channel-identification` (private attributes fixed at False), and `partial-results-stability` (hard-coded "high", which AWS describes as the fastest and least accurate setting). — *needs a call: Which subset to expose first, and whether `partial-results-stability` keeps "high" as its default. None can be probed end-to-end without account-side resources (a custom vocabulary, a custom language model), so they cannot ride an automatic PR.*
- [aws/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/stt/2026-08-31.md) — A dropped connection starts a new transcription session rather than resuming — The presigned URL carries no `session-id` or `session-resume-window`, so the reconnect that `WebsocketSTTService` performs opens an unrelated stream and any in-flight segment is lost. AWS allows resuming within a window of 1-300 minutes. — *needs a call: Whether resume is worth the state it adds, given a voice turn is short and a lost partial is usually re-spoken.*
- [aws/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/tts/2026-08-31.md) — run_tts reads the whole AudioStream before yielding, so TTFB is full synthesis time — measured on generative: first chunk 182 ms vs full read 1193 ms; end-to-end probe TTFB 671 ms (median of 5) vs 239 ms for standard. `AudioStream.iter_chunks()` is available on aiobotocore's stream — *needs a call: yielding partial audio changes when TTSAudioRawFrame first appears and how resampling is chunked; needs a call on reworking run_tts*
- [aws/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/tts/2026-08-31.md) — Polly's bidirectional streaming API (StartSpeechSynthesisStream, GA 2026-03-19) is not used — HTTP/2 stream that takes text incrementally and returns audio as synthesized, generative engine only; botocore ships the model but deletes the operation from the client (botocore/handlers.py remove_polly_start_speech_synthesis_stream), so it needs the smithy client aws_sdk_polly 0.11.0 — *needs a call: a streaming Polly class means a new class plus a new SDK dependency, the same shape as aws-nova-sonic's aws_sdk_bedrock_runtime extra — needs a product call*
- [aws/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/tts/2026-08-31.md) — The service leaves Engine unset, so Polly synthesizes with the standard engine — standard is Polly's oldest concatenative engine; neural measured at the same latency (TTFB 242 ms vs 239 ms, medians of 5) and Joanna supports it, but neural costs $16 vs $4 per 1M characters — *needs a call: a default engine change raises every user's bill 4x and breaks voices that only exist as standard; needs a maintainer's call*
- [azure/image](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/image/2026-08-31.md) — Settings expose only `image_size`; the v1 images request documents quality, background, output_format and more — Azure's v1 preview OpenAPI schema for `/images/generations` lists `quality` (low|medium|high|auto), `background` (transparent|opaque|auto), `output_format` (png|jpeg|webp), `output_compression`, `moderation`, `n`, `style`, `response_format`, `stream`/`partial_images`. `AzureImageGenSettings` adds only `image_size`; `n` is hard-coded to 1. — *needs a call: Which fields to expose depends on the endpoint rewrite above - the parameter set differs between the retired submit flow and the v1 endpoint, so adding fields now would encode the dead schema.*
- [azure/image](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/image/2026-08-31.md) — A non-2xx submit response raises `KeyError` instead of yielding an `ErrorFrame` — `operation_location = submission.headers["operation-location"]` runs unguarded; the header is absent on any error response, so every call against the retired endpoint dies with an unhandled `KeyError` rather than an `ErrorFrame`. The code carries a comment acknowledging this. — *needs a call: Whether to patch the error path on its own or only as part of the endpoint rewrite - a standalone fix touches code that is due to be replaced.*
- [azure/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/llm/2026-08-31.md) — Default gpt-4.1 is in Azure's "Legacy" lifecycle stage (retires 2027-04-14) — "Legacy" means Microsoft says newer models exist and workloads should plan to migrate; Azure names no replacement and will not until ~90-120 days before retirement. aiewf-eval aiwf_medium_context has quality-parity candidates (gpt-5.1 98.0%, gpt-5.6-sol 96.6%, gpt-5.6-terra medium 97.8% vs gpt-4.1 96.3%) but every one is more than 10% slower on TTFAT P50 (739-1098 ms vs 536 ms). On Azure the model string is a deployment name, so the default only matters for users who name their deployment after the model. — *needs a call: which GA tier, whether the TTFAT regression is acceptable, and that openai/llm and openai/responses-llm ship the same gpt-4.1 default; no Azure-side probe was possible because the test resource has no gpt-5.x deployment*
- [azure/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/llm/2026-08-31.md) — Azure's recommended Responses API has no Azure service class — Azure's v1 API docs recommend the Responses API for Azure OpenAI models, and OpenAIResponsesHttpLLMService already reaches it when base_url is the resource's /openai/v1/ endpoint (probed live, TTFAT 1157 ms vs 1860 ms for AzureLLMService chat completions on the same gpt-4.1 deployment). What is missing is an Azure entry point - no Entra ID token-provider path, and the WebSocket Responses class hardcodes wss://api.openai.com/v1/responses. — *needs a call: whether Azure gets a Responses subclass plus docs, or users are simply pointed at OpenAIResponsesHttpLLMService with base_url*
- [azure/realtime](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/realtime/2026-08-31.md) — CLI scaffold passes `input_audio_transcription=` to SessionProperties, a field the GA event model no longer has — `SessionProperties(input_audio_transcription=InputAudioTranscription(model="whisper-1"))` dumps to `{'type': 'realtime'}` — pydantic drops the unknown kwarg — so scaffolded Azure bots run with no user transcription. The GA shape is `audio=AudioConfiguration(input=AudioInput(transcription=...))`, as `examples/realtime/realtime-azure.py` uses. On Azure the transcription `model` must also name a transcription deployment, not the literal `whisper-1`. — *needs a call: the scaffold needs a transcription deployment name from somewhere, so fixing the shape alone still leaves a literal that only works by coincidence*
- [azure/realtime](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/realtime/2026-08-31.md) — Reasoning settings are stripped for Azure deployments of gpt-realtime-2 because the check reads the deployment name — `OpenAIRealtimeLLMService._strip_unsupported_reasoning` keeps `reasoning` only when `settings.model` contains `gpt-realtime-2`/`gpt-realtime-2.1`. On Azure that string is the deployment name, so a `gpt-realtime-2` deployment called anything else silently loses `Reasoning(effort=...)` (with a warning). Azure documents gpt-realtime-2 reasoning support. — *needs a call: how Azure should decide reasoning capability when the model name is opaque — trust the user (no stripping on Azure) or add an explicit opt-in*
- [azure/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/stt/2026-08-31.md) — Semantic segmentation (`Speech_SegmentationStrategy="Semantic"`) is not reachable from Settings — one more `set_property` pass-through alongside `segmentation_silence_timeout_ms`; segments on sentence-ending punctuation instead of silence, which suits agent turn-taking — *needs a call: Azure does not offer it in every locale and it disables confidence scores and NBest lists — exposing it needs a stance on locales where it silently does nothing.*
- [azure/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/stt/2026-08-31.md) — Automatic language detection is not supported; the recognizer is always pinned to one locale — Azure needs `AutoDetectSourceLanguageConfig` passed to the `SpeechRecognizer` constructor (open-range form since SDK 1.50); `_on_handle_recognized` already reads `result.language`, so per-utterance language would flow through — *needs a call: a second recognizer construction path plus a stance on what `Settings.language` means when detection is on — bigger than a pass-through field.*
- [azure/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/stt/2026-08-31.md) — Phrase lists (keyword/entity biasing) are not reachable — `PhraseListGrammar.from_recognizer(...)` boosts names and domain terms; commonly asked for in voice agents — *needs a call: it attaches to the recognizer, not the config, so it has to be re-applied on every reconnect and cleared on update — decide whether that lives in `_connect` or a dedicated setting handler.*
- [baseten/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/baseten/llm/2026-08-31.md) — Default moonshotai/Kimi-K2.6 is superseded by moonshotai/Kimi-K3 (Baseten Model APIs since 2026-07-27) — K3 is Moonshot's flagship successor to the K2 family; on Baseten it reasons by default (probe: thinking 806 ms, TTFAT 1632 ms median of 5 vs 618 ms for K2.6). With `extra={"reasoning_effort":"none"}` K3 lands at 623 ms, i.e. parity. — *needs a call: aiewf-eval has not benchmarked Kimi K3 on aiwf_medium_context (K2.6 is there at 98.3% thinking-on / 93.9% thinking-off), and a bump would also have to decide whether to ship `reasoning_effort: none` by default.*
- [camb/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/camb/tts/2026-08-31.md) — MARS 8.1 beta models are missing from MODEL_SAMPLE_RATES, so their 48 kHz audio is labelled 22050 Hz — `mars-8.1-flash-beta` and `mars-8.1-pro-beta` are in the SDK's speech_model enum and are the API's documented default; `MODEL_SAMPLE_RATES.get(model, 22050)` falls back to 22050 for both, and the docs list both at 48 kHz. Camb also lists mars-flash as 22.05 kHz/48 kHz, which the single table entry cannot express. — *needs a call: a live probe to confirm the beta models' actual stream rate, which CAMB_API_KEY currently blocks; also a call on whether beta models belong in the table at all*
- [camb/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/camb/tts/2026-08-31.md) — Camb's `voice_settings` (speaking_rate, maintain_source_accent, enhance_reference_audio_quality) is not reachable from Settings — `StreamTtsVoiceSettings` is a documented parameter of the streaming TTS endpoint the service calls; `speaking_rate` (default 1) is the speed control users usually ask for. `run_tts` never builds the object. — *needs a call: a probe with the field set, which the rejected credential blocks; and a call on whether to expose speaking_rate alone or the whole nested object*
- [camb/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/camb/tts/2026-08-31.md) — The constructor's `sample_rate` is never sent to Camb, so a user-chosen rate mislabels the audio — `output_configuration` carries a documented `sample_rate` field, but the service only sets `format="pcm_s16le"`. A caller passing `sample_rate=` gets a warning and frames tagged at their rate while the stream stays at the model's native rate. — *needs a call: a call on whether to request the rate from Camb or keep the warning-only behaviour, plus a live probe of a non-native rate*
- [cartesia/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/tts/2026-08-31.md) — Cartesia's `locale` request field (regional accents like `en-GB`) is not reachable from Settings — the Sonic 3.6+ gate is gone now that both services default to sonic-3.6, but `locale` is still documented as mutually exclusive with `language`, which both services always send when set — *needs a call: a stance on how a `locale` setting suppresses the always-sent `language` field*
- [cartesia/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/tts/2026-08-31.md) — Cartesia's `accent` request field is not reachable from Settings — documented for sonic-3 and newer on both the WebSocket and HTTP endpoints; sets how a multilingual voice sounds while speaking the language — *needs a call: the valid accent values are behind Cartesia's authenticated capability guide, and each must match the voice's own capabilities — a probe needs that list first*
- [sarvam/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/llm/2026-08-31.md) — sarvam-105b-conversations is 11x faster to first answer token and is the variant Sarvam positions for voice agents — TTFAT 888 ms (median of 5) vs 10219 ms for sarvam-105b, both GA. Sarvam still calls sarvam-105b the flagship/recommended model, so this is not a documented successor relationship. — *needs a call: aiewf-eval's medium-context leaderboard has no Sarvam rows, so the quality half of a default bump is unmeasured*
- [sarvam/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/llm/2026-08-31.md) — `deepseekv4-flash` is served by /v2 but rejected by the `_SUPPORTED_MODELS` allowlist — GET /v2/models lists it and a direct call answered in 1120 ms with no reasoning pass on our key, but Sarvam documents neither the model nor its capabilities; the constructor raises ValueError for it today. — *needs a call: whether Pipecat should accept a model Sarvam has not announced, and which capability sets (reasoning, vision, wiki grounding) it belongs in*
- [sarvam/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/llm/2026-08-31.md) — Capability tables disagree with Sarvam's model docs — `_WIKI_GROUNDING_MODELS` and `_REASONING_MODELS` include gemma4, which Sarvam documents as supporting neither, and exclude sarvam-105b-conversations, for which /v1 accepted both reasoning_effort=low and wiki_grounding=true (HTTP 200). Users on the conversations model get an error frame instead of a request. — *needs a call: HTTP 200 does not prove the parameters take effect on the conversations model; needs a stance on validating against docs vs. what the API accepts*
- [sarvam/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/stt/2026-08-31.md) — CLI registry offers only SarvamSTTService, whose endpoint Sarvam labels legacy — Sarvam's streaming-api page says `saaras:v3-realtime` supersedes the legacy WebSocket for voice agents, and calls realtime beta; `sarvam_stt` in `cli/registry/service_metadata.py` maps only to `SarvamSTTService` — *needs a call: whether SarvamRealtimeSTTService should become (or join) the registry's Sarvam STT entry while Sarvam still marks realtime beta*
- [sarvam/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/tts/2026-08-31.md) — bulbul:v3's `dict_id` (pronunciation dictionary) is not reachable from Settings — a `dict_id` field on `SarvamHttpTTSSettings` passed through in `run_tts` and `_send_config` would do it; documented on both the REST body and the WebSocket config — *needs a call: probing it needs a dictionary created through the Pronunciation Dictionary API — decide whether Pipecat exposes the field without also managing dictionaries*
- [simli/video](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/simli/video/2026-08-31.md) — `enable_logging` is accepted but never used — simli-ai 2.x has no logging toggle — the constructor and the deprecated `InputParams` both take `enable_logging`, and the docs page advertises it, but it reaches neither `SimliConfig` nor `SimliClient`; the SDK dropped its logging flag in the 2.0 overhaul and only exposes the stdlib logger `simli_client` — *needs a call: a call between deprecating the argument and honouring it by configuring the `simli_client` logger*
- [simli/video](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/simli/video/2026-08-31.md) — Simli's avatar model (`SimliConfig.model`, fasttalk default or artalk) cannot be selected — the service hardwires `ServiceSettings(model=None)` and builds `SimliConfig` without a model, so artalk is unreachable; the SDK has offered the choice since simli-ai 0.1.14 — *needs a call: whether `Settings.model` should map onto the `SimliModels` enum, and what a runtime settings update should do given `SimliConfig` is built once in `__init__`*
- [smallest/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/stt/2026-08-31.md) — `finalize_on_words` and `max_words` are not reachable from Settings — Pulse's documented agentic pattern is `finalize_on_words=false` plus a client `finalize` per turn, which is exactly what the service already sends on VADUserStoppedSpeakingFrame. — *needs a call: these change when finals arrive, so exposing them needs a stance on whether the service should adopt the agentic pattern by default rather than just pass the flags through*
- [smallest/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/tts/2026-08-31.md) — Streaming continuation (context_id / continue / context_close) is not used — each run_tts() is an independent request, so the service reconstructs word-timestamp offsets by hand (_advance_word_timestamp_request); the API's context_id groups fragments into one utterance and would make that bookkeeping unnecessary — *needs a call: adopting contexts reshapes run_tts/flush_audio and the turn lifecycle, not a Settings pass-through — needs a design call*
- [smallest/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/tts/2026-08-31.md) — Buffering and turn-end timing knobs are not reachable from Settings — max_buffer_flush_ms, max_buffer_delay_ms and complete_backoff_ms (default 4000 ms) are documented request fields; complete_backoff_ms is what delays the `complete` status after the last chunk — *needs a call: two of the three only mean anything inside a context_id continuation, so they follow the contexts decision above*
- [soniox/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/soniox/stt/2026-08-31.md) — Soniox's real-time `translation` config object is not reachable from Settings — v5 streams translation tokens on the same socket; the config takes `{type: one_way, target_language}` or `{type: two_way, language_a, language_b}` — *needs a call: translated tokens arrive interleaved with originals and marked `translation_status`, so exposing this needs a decision on how translations reach the pipeline (separate frames, or filtered out of the transcript)*
- [soniox/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/soniox/tts/2026-08-31.md) — An idle SonioxTTSService reconnects every ~11s because Soniox closes connections that have not started a stream — the service connects in `setup()` but only sends a per-stream config when a turn context is created; Soniox closes an unconfigured connection after ~10s even while keepalives flow, and the base websocket reconnect loop immediately opens another one — *needs a call: fixing it means changing when the socket is opened (connect lazily on the first stream, or keep a stream open), which is a lifecycle change rather than a value*
- [speechify/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechify/tts/2026-08-31.md) — Requests carry no `Speechify-Version` header, so the API version is whatever the workspace defaults to — Speechify's versioning guide tells raw HTTP integrations to send `Speechify-Version` on every request; without it the server falls back to the workspace default, or to the oldest supported version when the workspace has none. Nothing breaks today — the two retirements on the calendar (Simba 1.6 at version 2026-09-21, the voice-cloning consent reshape at 2026-09-13) touch models and endpoints this class does not use. — *needs a call: which date to pin, whether users can override it, and who bumps it as Speechify mints new versions*
- [speechmatics/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/stt/2026-08-31.md) — `operating_point` is deprecated in favour of `model`, and every session logs the SDK's deprecation warning — Speechmatics' Models page says `operating_point` is deprecated in SaaS and maps to `model`; speechmatics-voice 0.3.0b8 drops it from `VoiceAgentConfig` for `model: Model`, so `Settings.operating_point` and `default_settings.model = self._config.operating_point.value` both need migrating with the pin bump — *needs a call: whether to wait for 0.3.0 GA (0.2.8 is still the latest stable) or add a `model` alias against the 0.2.x field now*
- [speechmatics/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/stt/2026-08-31.md) — Default base URL is `wss://eu2.rt.speechmatics.com/v2`, not the documented global endpoint — `global.rt.speechmatics.com` has been the all-regions endpoint since 2026-07-20 and routes to the nearest region; `eu2` is an undocumented alias of `eu.rt.speechmatics.com`. Probe against global succeeded at TTFB 1064 ms vs 1133 ms for the current default — *needs a call: routing to the nearest region changes which region processes audio, so changing the default needs a stance on data residency*
- [speechmatics/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/stt/2026-08-31.md) — `speech_segment_config` is rebuilt unconditionally, so `add_trailing_eos` and `pause_mark` are unreachable — `_build_config` assigns a fresh `SpeechSegmentConfig(emit_sentences=...)` after the `extra_params` loop, discarding both the preset value and anything a user set via `extra_params` — *needs a call: applying `emit_sentences` in place would let the SDK preset default (True) through and change behaviour for existing users, so this needs a decision on the default before the two other knobs can be exposed*
- [speechmatics/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/stt/2026-08-31.md) — VAD, smart-turn and end-of-turn tuning are not Settings fields — `vad_config` (enabled/silence_duration/threshold), `smart_turn_config` and `end_of_turn_config` shape the ADAPTIVE and SMART_TURN modes the service exposes as `TurnDetectionMode`, but are settable only through `extra_params` — *needs a call: these are nested SDK models; exposing them needs a stance on whether Settings takes SDK types directly or mirrors them*
- [speechmatics/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/tts/2026-08-31.md) — A sample rate other than 16 kHz makes every request 422 with an opaque error — the API only serves `pcm_16000`/`wav_16000`, but `_get_endpoint_url` builds `output_format=pcm_{sample_rate}`; with `sample_rate=None` (adopt the pipeline rate, commonly 24 kHz) the probe got `Service unavailable [422]` and no audio, after only a warning at construction — *needs a call: whether the service should pin 16 kHz regardless of the caller's sample rate or raise at construction — either is a behavior change*
- [tavus/video](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/tavus/video/2026-08-31.md) — Tavus renamed persona/replica to PAL/Face; the class still takes `replica_id` only — Tavus's own Pipecat guide shows `TavusVideoService(api_key=..., face_id=..., session=session)` with `TAVUS_FACE_ID`; that snippet raises TypeError against our constructor. The API itself still accepts `replica_id` and `/v2/personas/{id}` (probed today), so nothing is broken server-side. — *needs a call: a `face_id`/`pal_id` alias is a new constructor parameter plus a registry env-var change — needs a call on whether to follow the rename and how to deprecate `replica_id`*
- [tavus/video](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/tavus/video/2026-08-31.md) — Conversation `properties` are not reachable — `max_call_duration`, `apply_greenscreen`, recording, timeouts — `TavusApi.create_conversation` posts only `replica_id` and `persona_id`, so every Pipecat Tavus session takes the API defaults, including the 3600 s `max_call_duration` cap and the 300 s `participant_absent_timeout`. — *needs a call: which knobs to expose, and where — settings are runtime-updatable but the conversation is created once in `setup()`, and the value has to thread through TavusVideoService, TavusTransportClient and TavusApi*

<details><summary><b>Low</b> (49)</summary>

- [anthropic/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/anthropic/llm/2026-08-31.md) — A plain dict in `Settings.thinking` fails at request time with `'dict' object has no attribute 'model_dump'` — `Settings.from_mapping` coerces thinking dicts, but the dataclass constructor stores them uncoerced and `_process_context` calls `model_dump()` on them; still reproduces on 712d7643c — *needs a call: whether the coercion belongs in __post_init__ so every path accepts the dict form that from_mapping documents*
- [anthropic/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/anthropic/llm/2026-08-31.md) — `temperature`, `top_k` and `top_p` are rejected with a 400 on Claude Opus 4.7 and later models — the service forwards them whenever a user sets them, so a user who carries settings across a model switch gets a failed turn rather than a warning — *needs a call: whether the service should warn or drop them on models that reject them, or leave it to the provider's error*
- [assemblyai/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/assemblyai/stt/2026-08-31.md) — LLM Gateway post-processing of finalized turns (llm_gateway) is not exposed — JSON-stringified chat-completions config on the connect URL; results arrive as LLMGatewayResponse messages the service does not parse — *needs a call: running an LLM inside the STT service overlaps with the pipeline's own LLM stage — needs a product call before any plumbing*
- [assemblyai/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/assemblyai/stt/2026-08-31.md) — Settings carry fields AssemblyAI has deprecated or no longer documents — `language_code` was deprecated in favour of `language_codes` on 2026-07-03 but has no `.. deprecated::` marker; `formatted_finals` and `word_finalization_max_wait_time` are absent from the current AsyncAPI spec yet still sent on every connect — *needs a call: confirm with AssemblyAI whether formatted_finals and word_finalization_max_wait_time are still honoured before removing or documenting them*
- [asyncai/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/asyncai/tts/2026-08-31.md) — `speed_control` and `stability` are not reachable from Settings — Documented request fields (`speed_control` 0.7-2.0, `stability` 0-100). A raw request with both set returns 200 on all three models, but Async's model page says only `async_flash_v1.0` supports them, so on the default `async_flash_v1.5` they are silently ignored. — *needs a call: a stance on exposing settings that the default model accepts but does not honor*
- [aws/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/llm/2026-08-31.md) — Newer Converse request fields are not reachable: `serviceTier`, `outputConfig`, `Tool.systemTool` — `serviceTier.type` is one of priority/default/flex/reserved; `Tool.systemTool` is how Nova 2's built-in web grounding and code interpreter are requested, and the adapter only emits `toolSpec` tools — *needs a call: whether any of these matter for real-time voice, or belong in `additional_model_request_fields`*
- [aws/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/llm/2026-08-31.md) — `top_k` and `seed` from LLMSettings are silently ignored — _build_inference_config sends only maxTokens/temperature/topP/stopSequences, matching Bedrock's inferenceConfig, but docs.pipecat.ai lists top_k and seed among the service's settings — *needs a call: whether to route top_k through additionalModelRequestFields or drop the two from the documented settings*
- [aws/other](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/other/2026-08-31.md) — No in-repo example or test exercises AWSAgentCoreProcessor — The AgentCore samples live in pipecat-examples; `tests/` has only `test_aws_credentials.py`. Nothing in this repo can be run against a deployed agent, which is why this unit has no live probe. — *needs a call: Whether an AgentCore example belongs in this repo, given it needs a deployed agent runtime to run.*
- [aws/realtime](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/realtime/2026-08-31.md) — The SYSTEM_SPEECH content role is missing from the Nova Sonic adapter's Role enum — Nova 2 Sonic accepts SYSTEM_SPEECH as the `role` on a text contentStart to pick the transcription script for Hindi code-switching (Latin / Devanagari / mixed); the enum in `aws_nova_sonic_adapter.py` has only SYSTEM, USER, ASSISTANT, TOOL — *needs a call: a stance on whether Hindi script selection is worth a user-facing knob, and what it would be called*
- [aws/realtime](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/realtime/2026-08-31.md) — `endpointing_sensitivity` is typed `str` rather than the documented HIGH/MEDIUM/LOW set — a typo reaches the API instead of failing at construction; AWS documents exactly three values — *needs a call: whether to narrow the type now or keep it open for values AWS may add*
- [aws/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/stt/2026-08-31.md) — Nine AWS streaming language codes have no Language enum member — `en-AB` (Scottish English), `en-WL` (Welsh English), `ckb-IQ` / `ckb-IR` (Sorani Kurdish), `kab-DZ` (Kabyle), `fa-AF` (Dari), `sw-BI` / `sw-RW` / `sw-UG`. `Language.KU` exists but is Kurmanji, not Sorani. — *needs a call: Whether to add members to `pipecat.transcriptions.language` for dialects only AWS distinguishes.*
- [aws/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/tts/2026-08-31.md) — The SynthesizeSpeech LanguageCode parameter is not exposed — bilingual voices (Aditi, Kajal, Hala, Zayd) pick their language from LanguageCode; the service only emits an SSML `<lang>` wrapper built from the `language` setting — *needs a call: whether `language` should also feed LanguageCode or a separate field is added — the two can disagree*
- [aws/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/tts/2026-08-31.md) — OutputFormat is hard-coded to pcm, which Polly caps at 16 kHz — neural, long-form and generative voices synthesize at 24 kHz natively and only expose that through mp3/ogg_vorbis/ogg_opus; pipelines running at 24 kHz get 16 kHz audio upsampled by the service. Polly also added mulaw/alaw output on 2026-03-25 — *needs a call: reaching 24 kHz means decoding mp3 or ogg in the service; needs a call on the dependency*
- [aws/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/aws/tts/2026-08-31.md) — Speech marks (word timestamps) are not requested — SpeechMarkTypes returns word/sentence/viseme marks, but only as a separate json-format request, so word timing costs a second billed synthesis — *needs a call: whether doubling Polly requests is acceptable for word timestamps*
- [azure/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/llm/2026-08-31.md) — Azure-only chat-completions fields (data_sources, user_security_context) have no named Settings field — `extra` reaches the request body verbatim (`params.update(self._settings.extra)` in base_llm), so On Your Data and the Defender end-user context are usable today; not probed, since data_sources needs a provisioned AI Search index. This is discoverability, not capability. — *needs a call: whether `extra` stays the documented answer for Azure-only request fields*
- [azure/realtime](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/realtime/2026-08-31.md) — Azure's realtime transcription and translation session types are not reachable — Azure exposes dedicated flows for `gpt-realtime-translate` (`/openai/v1/realtime/translations`) and for transcription (`session.type = "transcription"`, models `gpt-realtime-whisper`, `gpt-live-transcribe`). The service only speaks the voice-agent session. — *needs a call: a call on whether these belong in Pipecat as a realtime STT/translation service rather than in this LLM class*
- [azure/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/stt/2026-08-31.md) — Monolingual post-stream refinement is not reachable — `SpeechServiceResponse_PostProcessingOption=PostRefinement` is GA for a single locale and documented as no impact to first-token latency — *needs a call: Azure describes the gain for long-form audio and says short phrases often come back unchanged — unclear it helps agent-length turns, so it needs a measurement before we expose it.*
- [azure/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/stt/2026-08-31.md) — Azure's newest STT models (mai-transcribe-1.5) are not reachable from Pipecat — they live on the LLM Speech API (file transcription, public preview) and Voice Live, not the streaming Speech SDK that AzureSTTService uses; mai-transcribe-1 is deprecated as of 2026-08-20 — *needs a call: reaching them means a separate non-streaming service class or a Voice Live integration — a product call, not a change to this class.*
- [azure/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/stt/2026-08-31.md) — The `azure` extra floor (`azure-cognitiveservices-speech>=1.47.0`) predates the 1.48.2 CRL fix — Microsoft asked Linux/Android users with CRL checking enabled to be on 1.48.2+ before 2026-07-01 or hit X509_V_ERR_DIFFERENT_CRL_SCOPE; fresh resolves already get 1.51.x, so only pinned-old installs are exposed — *needs a call: whether provider-watch may move dependency floors, or whether that is a maintainer-owned change.*
- [camb/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/camb/tts/2026-08-31.md) — Camb's `inference_options` (stability, temperature, inference_steps, speaker_similarity) is not reachable from Settings — MARS-8-family-only tuning knobs, documented as unsupported on the two 8.1 beta models. — *needs a call: a stance on per-model applicability — the fields silently do nothing on the beta models*
- [camb/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/camb/tts/2026-08-31.md) — `enhance_named_entities_pronunciation` is not exposed — Top-level boolean on the streaming TTS request, default false; a one-field pass-through. — *needs a call: a live probe with the field set, which the rejected credential blocks*
- [cartesia/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/stt/2026-08-31.md) — `Cartesia-Version` is pinned to 2026-03-01 while the STT API reference documents 2026-08-14 — 2026-03-01 still connects and transcribes on both models, so nothing is broken; the same pin appears in the turns service — *needs a call: what 2026-08-14 changes for STT — Cartesia's API-versioning page is behind a login, so the diff could not be read*
- [cartesia/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/tts/2026-08-31.md) — `add_phoneme_timestamps` is not exposed — Cartesia can return phoneme-level timestamps alongside the word timestamps the service already consumes — *needs a call: a use for phoneme timestamps in the pipeline (viseme/lipsync output) before adding a setting nothing reads*
- [cartesia/turns-stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/turns-stt/2026-08-31.md) — Threshold changes reconnect instead of using the documented mid-session `config` command — the endpoint accepts a `config` text message carrying a `turn` object to retune turn detection on a live socket; the branch reconnects instead, matching how a changed keyterm list is applied — *needs a call: whether a threshold change mid-call is worth a second update path, and confirmation the config command is honored under the API version the service pins*
- [cartesia/turns-stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/turns-stt/2026-08-31.md) — The turns endpoint documents `cartesia_version` 2026-08-14; the service pins the 2026-03-01 header — the AsyncAPI spec lists `cartesia_version` as a required query parameter whose only value is 2026-08-14, but the pinned 2026-03-01 header connects and transcribes normally, and no changelog entry describes what 2026-08-14 changes — *needs a call: what the 2026-08-14 version changes for STT before moving a header two other Cartesia services also pin*
- [cartesia/turns-stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/cartesia/turns-stt/2026-08-31.md) — `turn.eager_end` is surfaced only as an event handler; nothing in the pipeline speculates on it — Cartesia positions eager end as the latency win of Ink 2 — start generating on `turn.eager_end`, discard on `turn.resume`; `DeepgramFluxSTTService` exposes its equivalent the same way, so this is a pipeline-level gap rather than a Cartesia one — *needs a call: a framework design for speculative LLM turns (and its cancellation) before any service can do more than raise the event*
- [sarvam/llm](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/llm/2026-08-31.md) — `sarvamai` is pinned to ==0.1.28 while 0.1.31 is current — The pin is on the shared sarvam extra (the LLM service itself uses the openai SDK); sarvamai publishes no release notes, so the delta is unread. — *needs a call: coordinating with the Sarvam STT/TTS services that actually import the SDK*
- [sarvam/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/stt/2026-08-31.md) — sarvamai pinned to ==0.1.28, three releases behind 0.1.31 — `speech_to_text_streaming.connect()` is byte-for-byte identical between 0.1.28 and 0.1.31; the only STT change is the model Literal, which drops `saarika:v2.5` and adds `saaras:v4` and is `Any`-unioned, so nothing we send changes — *needs a call: whether an exact SDK pin gets bumped when no STT-relevant API changed*
- [sarvam/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/stt/2026-08-31.md) — Language maps cover 12 of the 24 codes Sarvam accepts — unmapped codes still reach Sarvam through `resolve_language`'s fallback but log "not verified"; ks-IN, sat-IN, mni-IN, brx-IN, doi-IN and ne-IN have no `Language` member at all, so closing this touches the shared enum — *needs a call: whether to add the missing `Language` members for Sarvam's smaller scheduled languages*
- [sarvam/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/tts/2026-08-31.md) — `SarvamTTSSpeakerV3` is out of step with Sarvam's v3 voice list — it names `amelia` and `sophia`, which Sarvam no longer accepts, and omits 14 current voices (anand, tanya, tarun, sunny, mani, gokul, vijay, shruti, suhani, mohit, kavitha, rehan, soham, rupali); `voice` is a free string, so nothing is gated by it — *needs a call: removing two members from a public StrEnum — decide whether that needs a deprecation cycle*
- [sarvam/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/tts/2026-08-31.md) — `bulbul:v3-beta` is still a SarvamTTSModel member and a TTS_MODEL_CONFIGS key — Sarvam promoted the tag to `bulbul:v3` in February 2026 and its docs no longer list it; the endpoint still serves it, so nothing breaks today — *needs a call: whether to deprecate the enum member and table entry or leave the pre-GA tag working*
- [sarvam/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/sarvam/tts/2026-08-31.md) — 12 of the 23 language codes Sarvam TTS accepts are not in language_to_sarvam_language — the API validates against as-IN, brx-IN, doi-IN, kok-IN, ks-IN, mai-IN, mni-IN, ne-IN, sa-IN, sat-IN, sd-IN, ur-IN on top of the 11 mapped ones, but each answers 'Please request beta access'; 5 of them have no Language member at all — *needs a call: whether to map languages that need per-account beta access, and whether to add the missing Language members*
- [simli/video](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/simli/video/2026-08-31.md) — `handleSilence` is not reachable from the service — documented on both `SimliConfig` and the `/compose/token` request; with it off the avatar freezes instead of idling when the input buffer drains — *needs a call: the same construction-time question as the model setting — a `Settings` field would look live but only take effect at `__init__`*
- [simli/video](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/simli/video/2026-08-31.md) — Transport and retry options are fixed — `enableSFU=True` is hard-coded and `transport_mode` (P2P or LIVEKIT), `retry_count` and `retry_timeout` are not exposed; only `simli_url` can be varied — *needs a call: whether anyone wants the LiveKit transport from Pipecat, given Pipecat already owns the WebRTC leg*
- [simli/video](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/simli/video/2026-08-31.md) — Simli's own Pipecat page documents a constructor signature we no longer have — https://docs.simli.com/api-reference/pipecat.md instantiates `SimliVideoService(SimliConfig(...))`; the current class takes `api_key` and `face_id` — *needs a call: someone to file it with Simli — the page is in their docs, not ours*
- [smallest/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/stt/2026-08-31.md) — `vad_events` speech_started / speech_ended messages are not surfaced — `vad_events=true` interleaves acoustic `speech_started` / `speech_ended` JSON messages with transcriptions; `_process_response` reads only `transcript`, so they would be dropped. — *needs a call: decide whether server VAD events should map to Pipecat frames at all, given the pipeline already runs its own VAD*
- [smallest/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/stt/2026-08-31.md) — `numerals=auto` is sent on every connection but is not in Pulse's parameter reference — The AsyncAPI spec for GET /waves/v1/stt/live lists no `numerals` parameter; `itn_normalize` is the documented way to get written-form numbers. — *needs a call: confirm with Smallest whether `numerals` is still honored before dropping the field or redirecting users to `itn_normalize`*
- [smallest/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/stt/2026-08-31.md) — `punctuate` and `capitalize` are not reachable from Settings — Both default to true and split what the single `format` field controls today. — *needs a call: decide whether the finer-grained pair is worth two fields next to `format`, which the docs still present as the one formatting switch*
- [smallest/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/stt/2026-08-31.md) — Pulse Pro has no Pipecat path — pulse-pro is GA (English-only, 5.42% ESB WER, #2 on the Open ASR Leaderboard) but batch-only; the streaming endpoint rejects it with 400. Reaching it needs an HTTP class against POST /waves/v1/stt/. — *needs a call: a new service class is out of scope for provider-watch; decide whether a batch Smallest STT service is wanted*
- [smallest/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/tts/2026-08-31.md) — number_pronunciation_language and math_notation are not reachable from Settings — both are documented top-level fields on the WebSocket request and would be straight pass-throughs in `_build_msg` — *needs a call: neither is documented on the Lightning model cards, only in the WebSocket API reference — confirm they are live on lightning_v3.1_pro before exposing them*
- [smallest/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/smallest/tts/2026-08-31.md) — `auto` language routing is not reachable through the Language enum — the API accepts the `auto` language code for per-request detection and mid-sentence switching; users can only get it by passing the raw string, since Language has no AUTO member — *needs a call: whether to add a Language.AUTO member or document the raw-string escape hatch is a framework-wide call, not a Smallest one*
- [soniox/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/soniox/tts/2026-08-31.md) — `bitrate` is not reachable, so Soniox's compressed audio formats cannot be configured — `audio_format` is a constructor argument that accepts mp3/opus/aac, but `_build_config_msg` sends `sample_rate` only for PCM and never `bitrate` — *needs a call: the receive path wraps Soniox bytes in `TTSAudioRawFrame` without decoding, so compressed formats are unusable downstream anyway — the call is whether to expose `bitrate` or to reject non-PCM `audio_format` outright*
- [soniox/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/soniox/tts/2026-08-31.md) — `client_reference_id` (usage-log attribution) is not reachable from Settings — documented optional string recorded with the request in Soniox usage logs; ignored for temporary API keys — *needs a call: whether per-request usage tagging belongs in Settings or is left to the Soniox console*
- [soniox/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/soniox/tts/2026-08-31.md) — docs.pipecat.ai names `Maya` as a Soniox voice, which only exists on the retired tts-rt-v1 — the tts-rt-v2 catalogue has no `Maya`; the shipped default voice `Bryce` is correct — *needs a call: the page lives in the docs repo, so it is a separate change from this one*
- [speechmatics/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/stt/2026-08-31.md) — Language and output-locale maps are behind the provider's list — `BASE_LANGUAGES` omits `tl`, `ia` and the bilingual packs `ar_en`, `en_ms`, `cmn_en`, `cmn_en_ms_ta`, `en_ta` (they still work, via `resolve_language`'s pass-through plus a 'not verified' warning); `LOCALES` covers only en-GB/en-US/en-AU while the docs also list Mandarin and Portuguese output locales — *needs a call: the bilingual pack codes have no `Language` enum member, so covering them means adding members or documenting the raw-string route*
- [speechmatics/stt](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/stt/2026-08-31.md) — Remaining VoiceAgentConfig options and the fast/captions/scribe presets are only reachable indirectly — `enable_entities`, `max_delay_mode`, `transcription_update_preset`, `audio_buffer_length` and `advanced_engine_control` work through `extra_params`; `TurnDetectionMode` covers four of the SDK's seven presets — *needs a call: whether the `extra_params` escape hatch is considered sufficient for these, and whether non-agent presets belong in a turn-detection enum*
- [speechmatics/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/tts/2026-08-31.md) — Service requires a caller-supplied `aiohttp_session` — alone among Pipecat's HTTP TTS services; probe.py cannot construct it, so this unit needs an ad-hoc probe every run — *needs a call: making the session optional means owning its lifecycle in the service — a constructor change the team may not want*
- [speechmatics/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/tts/2026-08-31.md) — TTS is still a free preview on `preview.tts.speechmatics.com` — Speechmatics' quickstart still says "currently in preview and free to use. Features and pricing may change"; the hard-coded `base_url` default will need revisiting at GA — *needs a call: nothing to do until Speechmatics announces GA and a stable hostname — re-check each run*
- [speechmatics/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/speechmatics/tts/2026-08-31.md) — Speechmatics' own Pipecat TTS page documents the deprecated `voice_id=` argument — docs.speechmatics.com/integrations-and-sdks/pipecat/tts shows `voice_id="sarah"` instead of `settings=SpeechmaticsTTSService.Settings(voice=...)` — *needs a call: the page lives in Speechmatics' docs repo; someone has to raise it with them*

</details>

## Did not complete

- [azure/realtime](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/azure/realtime/2026-08-31.md) — AZURE_REALTIME_BASE_URL points at a resource host that no longer resolves (DNS NXDOMAIN under .cognitiveservices.azure.com, .openai.azure.com and .services.ai.azure.com), and it is still in the retired `/openai/realtime?api-version=2024-10-01-preview&deployment=...` form, so no connect probe could run.
- [bland/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/bland/tts/2026-08-31.md) — BLAND_API_KEY is not set, so no live probe could run
- [camb/tts](https://github.com/pipecat-ai/provider-watch-reports/blob/main/reports/camb/tts/2026-08-31.md) — CAMB_API_KEY is rejected by Camb.ai (401, "Invalid/Expired API Key"), so no probe could run and no change could be verified.

---
To record a decision about an item above, reply on this issue naming the service and enough of the item to identify it, one decision per line — e.g. `deepgram/stt, diarize_model: skip, the extra= workaround is fine` or `openai/realtime, tool_choice: tracked in #5400, stop reporting`. Fixes need no comment: the next run sees the code change. It reads these comments for everything it cannot see — won't do, later, tracked elsewhere — and records each decision in the service's `decisions.md`, beside its reports. Full triage workflow: the [README](https://github.com/pipecat-ai/provider-watch-reports#the-weekly-loop).
